# 思玥 Siyue · 设计规范

**2026-09-07 设备约束：iPhone 与 iPad 必须同时适配。当前重视手机，iPad 是未来主力；新页面和布局修改同时纳入两类设备，不将平板适配后置。具体矩阵见 [UI 设计与验收](docs/design/ui-acceptance.md)。本条是要求，不代表已有 iPad 验收。**

状态：维护者于 2026-09-09 确认 Sage 方向：暖鼠尾草与燕麦色的浅色卡片层次，搭配绿色操作按钮。此决定替代此前纯黑白灰及 R3 蓝灰焦点卡色板；图标、分组列表和已修正的导航关系继续保留。当前进行既有页面、Figma 组件与实例的主题统一，仅明色、暗色两种主题；新主题尚未完成整体视觉验收。

## 验收入口

开发验证按 [自动化优先策略](docs/testing.md)：Playwright 能覆盖并通过的路径暂不 Computer Use；原生脚本仍无法覆盖的缺口再定向操作模拟器。行为通过与视觉/原生验收分开记录。

详细标准见 [UI 设计与验收](docs/design/ui-acceptance.md)：尺寸/排版基线、UI-V 视觉清单、UI-I 交互清单、S/M/L 验收分级和可复制证据模板。它是后续实施基线，不代表当前页面已全部达标。

## 当前视觉规则

先统一现有页面与 Figma 实例，再扩展其他 UI。配色方向已获维护者确认，不等于下列组件、实例或各端页面已经逐项验收。

- 中文与英文均为必需界面语言。布局同时容纳中文与英文长度差异，按钮、导航、表单、消息状态及弹层不得因翻译裁切或使操作不可达；字号放大、手机和平板尺寸分别检查。文案与无障碍名称同步本地化；具体矩阵见 UI 验收文档。

- 产品名称仅在移动侧边栏顶部显示；聊天顶栏、空状态、消息署名及设置列表不重复品牌名称。
- 简单文案、入口删减做必要静态检查即可；较复杂的布局和交互改动再进行模拟器截图验收。

- 以暖鼠尾草、燕麦色组织浅色卡片层次，绿色突出主要操作、选择与进度。内容层级仍通过留白、排版和分组表达；不将所有卡片填成高对比主色，也不以颜色代替图标或文字状态。
- 仅两种主题：明色、暗色。设置页提供切换，选择保存在本机；首次未选择时以设备当前明暗外观初始化，不增加第三种主题选项。
- 明色采用暖浅背景、近奶油白内容区、浅鼠尾草焦点卡和深绿灰文字；暗色采用深绿灰背景与表面、浅色文字和浅绿操作色。两种主题分别配对验证，不机械反转色值。
- 按钮背景、按压背景与文字成对定义；焦点卡背景不兼作按钮或完成状态背景。错误使用独立错误色，不能因主按钮为绿色而把错误提示变绿。错误和选中状态保留明确文字、图标或边框，不只依赖颜色。
- 使用系统字体、分组列表与克制的圆角；设置采用首页摘要、独立配置页、可搜索的供应商选择表单。
- 移动色值维护于 `apps/mobile/src/ui/theme.ts`；组件通过 `useTheme()` 响应切换，不复制固定色号到页面。桌面以 `apps/desktop/src/renderer/styles.css` 的语义变量映射同一视觉角色；平台实际切换行为与验收状态分别记录。

### 语义变量与 Figma 基础

当前色板及 Figma 变量 ID 见 [Sage foundations](docs/design/sage-theme-foundation-2026-09-09.json)。Figma semantic collection 为 `547:44`，明色、暗色模式分别为 `547:1`、`547:2`；语义键与移动 `theme.color` 同名对应。复用入口为 [Sage 组件页](https://www.figma.com/design/5OKY7OMJ2jQy2XAaGbNDGC?node-id=548-33774)（`548:33774`）。变量及组件页存在不代表所有页面已绑定或已完成视觉验收。

| 用途 | 语义变量 | 使用边界 |
|---|---|---|
| 页面与内容表面 | `background`、`surface`、`subtle` | 背景、卡片及次要表面分别使用 |
| 内容文字 | `ink`、`muted` | 正文与辅助文字，均检查实际背景对比度 |
| 焦点卡 | `focus`、`focusPressed`、`onFocus`、`focusMuted` | 浅卡与暗卡使用各自配对文字；按压不降低整卡透明度 |
| 主要操作 | `accent`、`accentPressed`、`onAccent` | 主按钮及完成 checkbox，不复用浅卡背景 |
| 进度 | `focusTrack`、`focusProgress`、`progressTrack`、`progress` | 焦点卡和普通内容的轨道与填充独立配对 |
| 边界与选中 | `border`、`controlBorder`、`selectedBorder` | 装饰分隔与必要控件边界不可混用 |
| 错误 | `error` | 错误文字保留明确语义，不跟随主操作色 |

新建或更新 Figma 实例时，绑定对应语义变量与明暗模式，复用组件页已有组件；仅缺少组件时补建。正文、标签及必要提示至少 4.5:1，识别控件或状态所需的图形至少 3:1；同时检查按压、选中、输入和错误状态。对比度测试通过不替代页面观感与交互验收。

## 信息密度与文案

维护者于 2026-09-06 确认：产品面向年轻用户，以简洁、易理解为基线，优先让内容和当前操作获得注意力。

- **图标优先**：设置、新建对话、侧栏、返回等熟悉操作优先使用通用图标；不默认使用大块文字按钮或同时堆叠图标与文字。设置用齿轮，新建对话用撰写图标，同一操作跨页面保持一致。
- **保留可识别性**：图标含义不明确时才补充短标签。纯图标按钮必须有准确的无障碍名称，触控区域 iOS 至少 44 × 44、Android 至少 48 × 48；不要用缩小点击区域换取视觉简洁。
- **标题按需出现**：不为每个区域添加标题。页面上下文已经明确时，删除重复页名、分类名和小标题；例如聊天首页不必再标注“AI 对话”。产品名只在侧栏顶部出现。
- **文案能删则删**：标题、描述、辅助文字、标签和行内文本（包括 span）都用最短且准确的表达。删除口号、重复解释、对显而易见操作的教程，以及没有新增信息的描述；不为填补留白而写文案。
- **说明按需展示**：少见配置的帮助就近展开；同一说明只放在最相关的位置，不在首页、侧栏和设置重复展示。空状态保留有助于开始操作的最少内容。
- **突出当前任务**：主要注意力留给对话内容与输入。设置等低频入口收在侧栏；克制使用高对比填充、强调字重和装饰，避免次要入口抢占焦点。
- **呈现真实对象与交互**：页面使用统一图标、成员头像、进度和独立任务行表达导航、身份与工作状态；记录可进入详情，任务可按权限编辑或完成。不要用整段功能说明加统一大按钮替代产品界面，也不为填空增加头像或进度装饰。
- **必要信息不省略**：错误恢复、破坏性操作后果、费用与数据发送范围等影响用户决定的信息用简短文字说清楚；简洁不等于含糊或隐藏状态。

审查时逐项判断：删掉这段文字是否影响理解？图标能否清楚表达？信息是否重复？次要控件是否抢过内容？只有确有作用的元素才保留。

## 应用结构与组件

- React Native + Expo；导航由 Expo Router 的 Stack、Drawer 组成。当前侧栏包含对话及“我的空间”入口，不显示底部导航；设置从侧栏进入独立页面。“我的空间”已接入本地已保存目标的查看及任务完成、改名，首页、详情、编辑采用不同导航层级。初始化的目标示例和原生组件预览不再进入正式导航；现有入口不代表账号、家庭共享或云同步已经实现。
- assistant-ui 官方 Expo 示例提供聊天基础，源码来源与许可见 `apps/mobile/src/chat/UPSTREAM.md`。普通原生控件使用 Expo UI。
- 聊天使用用户配置的 OpenAI 兼容接口；首页不放设置引导；设置从侧栏进入，未配置时不自动返回 Mock。会话仅本次运行保留；旧目标示例 UI 已退出正式应用，底层 SQLite、草稿与确认逻辑及已有数据保留。设置页覆盖外观、AI 服务与本机数据说明。
- 桌面继续 Electron + React；现有目标工作台与移动页面采用同一 Sage 语义方向。各端实际覆盖范围、主题切换和验收结果以实现及证据为准，不用单端截图证明跨端完成。

## 设计工作方式与历史材料

- 本轮账号、个人及家庭空间流程先完成 OpenSpec 提案，再用 Figma 定义交互草稿；经维护者确认后细化视觉并实施正式 UI，覆盖旧 huashu-design 原型优先约定。huashu-design 保留为其他已授权设计探索的方法，禁止使用 ui-ux-pro-max。
- `docs/design/references/headspace/` 保留此前选择的原始材料与许可，作为历史参考，不再作为当前色板或主题规范。
- 不使用 Headspace 品牌资产、商业字体或冥想功能。不默认运行 Spectr；design-md 通用技能未自动采用。
- 新流程或明显改变方向先原型评审；已有方向内的小修改直接实施。具体按 UI 验收分级及 OpenSpec 流程执行。
