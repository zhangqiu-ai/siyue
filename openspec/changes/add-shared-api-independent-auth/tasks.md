## 1. SA-01 基线

- [x] 1.1 保存三份原件，逐字节和 SHA-256 核对，建立项目入口。
- [x] 1.2 核对 feature/dev@fee1f4b、已有白板和会话边界，登记方案快照差异。
- [x] 1.3 完成新契约与现有调用方差异审查，运行严格规格与版本检查，记录证据。

## 2. SA-02 数据库

- [x] 2.1 实现独立 provision、pg adapter 和配置，以真实角色负面测试验证最小权限。
- [x] 2.2 实现锁、checksum 与事务迁移，以真 PostgreSQL 验证空库、重跑、并发与篡改拒绝。
- [x] 2.3 接入 live/ready 与启动失败关闭，验证断库、schema、秘密缺失与 Mock 发布禁止。

## 3. SA-03 会话

- [x] 3.1 实现签名及状态验证，运行签名、算法、产品隔离、过期和撤销负面测试。
- [x] 3.2 实现唯一后继和 60 秒加密恢复，运行真 PG 并发、重放及丢响应测试。
- [x] 3.3 实现设备退出与单次 action-bound reauth，验证原子消费及权限拒绝。
- [x] 3.4 接入既有 SessionVerifier 和新认证接口，运行 Playwright HTTP 回归与旧客户端测试。

## 4. 交付

- [x] 4.1 更新证据、工作包与版本状态，运行类型、相关测试、spec:check、release:check 和 diff 检查。
- [x] 4.2 对照原件登记 SA-04/05 接口依赖；邮件、Apple、原生、家庭、五设备与部署分别验收。

本批证据与实际限制见 [2026-09-21 首批证据](../../../docs/evidence/backend-auth-foundation-2026-09-21.md)。SA-04～10 和整体账号、家庭、五设备验收继续推进，未归档。

## 5. SA-04 邮箱闭环

- [x] 5.1 增加严格邮箱/密码合同和第二条迁移，验证码点边界、唯一约束、历史迁移兼容。
- [x] 5.2 实现有界 Argon2id、注册挑战、幂等与累计限流，验证错码计数、并发消费、跨用途和重复请求。
- [x] 5.3 实现登录、密码重置和密码再次验证，验证统一拒绝、版本变化、旧会话/授权失效及无自动登录。
- [x] 5.4 实现独立邮件worker与SMTP模板适配，验证发送、明确失败、结果未知、重启和载荷清理；真实送达单独验收。
- [x] 5.5 运行真PostgreSQL、Playwright HTTP及原回归，更新证据和配置说明，不提升真实邮件/Apple/原生验收。

SA-04 上述勾选仅对应本地实现和自动化范围，见 [本批证据](../../../docs/evidence/backend-auth-email-2026-09-21.md)。原方案工作包还要求真实邮箱送达，因此 SA-04 整体保持待验收；客户端、Apple、账号安全、家庭、五设备及部署继续推进，活动变更不归档。

2026-09-23 增补改密已提交后旧 access 失效造成的幂等重试缺陷；修复、原 Postgres 回归和 HTTP E2E 证据见[改密幂等重试](../../../docs/evidence/backend-auth-password-change-idempotency-2026-09-23.md)。改密重试与单次副作用本地自动化通过；不改变 SA-04 的真实邮件门槛，也不代表 SA-07 整体完成。

## 6. SA-05 客户端

- [x] 6.1 严格 typed API 与认证状态机，单次刷新、持久化 rotationId、代际隔离和离线撤销队列；真实 HTTP 并发及错误测试。
- [ ] 6.2 接入移动 SecureStore 与桌面主进程加密 vault，受限 IPC，验证损坏／不可用／写入失败不明文回退。
- [ ] 6.3 按已批准设计与本轮密码合同接入登录、注册和找回页面，中英／明暗与 iPhone/iPad／Android／Electron 验收。
- [ ] 6.4 完成账号范围缓存与本地空间显式映射、换号、离线及平台生命周期证据，不提前勾整体验收。
  - [x] 6.4a 实现独立持久空间映射、受信会话驱动的空间协调器及本地客户端生命周期；真实 HTTP/PG 会话与 SQLite 双账号、重启、迟到响应验证。
  - [ ] 6.4b 将空间协调器接入正式移动/桌面宿主和显式创建／选择入口，隔离待处理命令及界面缓存，保留未保存输入。
  - [ ] 6.4c 接入 Excalidraw 命名空间、拍照原件目录、切换前保存与旧 DOM 会话失效；验证换号和重启不串图、不覆盖原件。
  - [ ] 6.4d 完成既有本地空间显式关联／恢复与完整平台验收，不以新建空账号空间替代既有内容控制能力。

SA-05 会话基础已接入原生宿主与 Electron 主进程，见 [会话基础证据](../../../docs/evidence/backend-auth-client-2026-09-22.md)。后续登录与找回密码接入设置入口，桌面实际流程及 iPhone/iPad/Android 原生恢复通过，见 [界面证据](../../../docs/evidence/backend-auth-account-ui-2026-09-22.md)。6.2 仍缺完整原生故障、真机及 Linux/Windows 系统存储证据；6.3 仍缺注册与完整 UI 矩阵；6.4 账号空间映射继续实现。各项保持未勾选，整体未验收。

2026-09-23 当前工作树回归复核见[本轮证据](../../../docs/evidence/backend-auth-validation-2026-09-23.md)：Adapters 82/82、移动类型检查、账号 UI／邮箱 HTTP Playwright 6/6、spec 6/6、release 与 diff 检查通过。HTTP 注册通过不替代注册 UI 验收；正式协议正文和版本未提供，6.3 仍门控、未完成。

2026-09-23 增加邮箱账号的当前密码再次验证与改密流程，复用服务器 action-bound grant 和幂等接口；Electron 受限 IPC 不传递 access token，React Native 复用共享控制器。响应未知时仅在内存保留原操作并锁定表单，安全恢复记录只在服务器确认成功后清除；同进程重新打开面板可继续重试。中英 Electron Playwright 覆盖错密码、确认不匹配、服务端已提交但响应丢失、同幂等 key 恢复、旧会话撤销和新密码登录。见[改密客户端闭环证据](../../../docs/evidence/backend-auth-password-change-client-2026-09-23.md)。这只补 6.3 的邮箱改密切片；注册仍受正式条款与隐私文案门控，移动原生 UI / 真机及 6.3 整体验收未完成，不勾选 6.3。

2026-09-23 增加 SQLite 非秘密初始化标记，避免 Expo SecureStore 在 Android KeyStore key 丢失后返回 null 时被误判为首次安装；iOS 专用 QA XCTest 与 Android API 36 QA Release 流程均由应用自身删除模拟 SecureStore 项后冷启动验证损坏提示，Android 两次冷启动和中英页面通过；iOS UI 3/3、Android 缺项场景2/2、移动单测143/143、类型检查通过。具体命令、截图、结果文件及边界见 [安全存储缺项复验证据](../../../docs/evidence/backend-auth-client-2026-09-23.md)。模拟器删除条目不等同于实际 Keystore 解密故障；此证据不代替真机、系统存储异常或 Windows/Linux 验收，6.2 继续未勾选。

6.4a 的测试宿主结果见 [空间接线基础证据](../../../docs/evidence/backend-auth-account-space-2026-09-22.md)。桌面已增加真实设置入口的A/B账号空间、业务记录与白板笔迹隔离和重启回归；切换前白板落盘失败保护与重试通过。移动端已接入Provider与页面，Android账号空间创建、白板笔迹及相册图片的后台返回/重启/换号回归通过；iOS白板、相机原件、完整失败路径、表单恢复与原空间关联仍未完成，因此6.4b/c/d保持未勾选。详见同一证据文件的桌面增量记录。

2026-09-23 桌面登录/启动回归发现 workspace revision 变化后旧 LocalClient 被取消而页面未重新读取；已在 revision 稳定后重新载入快照，认证和账号空间 Playwright 28/28 通过。记录见 [Electron revision 恢复证据](../../../docs/evidence/backend-auth-account-space-2026-09-22.md#electron-登录时工作区-revision-的重新读取)。其他平台及 SA-05 完整验收仍待完成。

2026-09-23 真实 Electron Playwright 覆盖本机/A/B 未提交目标与项目名称在登出、切换账号及返回时的保留和隔离，并发现认证工作区重挂时设置对话框丢失账号子页；修复后重建 renderer，账号空间 Playwright 1/1、桌面登录 1/1、desktop 单测23/23及类型检查通过。该证据只覆盖桌面当前进程，完整原生移动输入、待处理命令与 6.4b 总验收仍待执行，不勾选6.4b。详见[账号切换与未提交输入证据](../../../docs/evidence/backend-auth-account-switch-2026-09-23.md)。

2026-09-23 Android 原生 QA 的中英舍弃用例发现计划页清空状态可能在页面卸载前尚未写入空间草稿 buffer；已改为同步写入并在明确舍弃时清理该空间的未提交计划字段。API 36 模拟器 QA Release + UIAutomator 2/2、移动类型检查和单测143/143通过。该结果只覆盖明确舍弃，不覆盖切换账号时的输入隔离/恢复；6.4b 继续未勾选。详见[Android 计划输入舍弃证据](../../../docs/evidence/backend-auth-mobile-draft-discard-2026-09-23.md)。

2026-09-23 Android API 36 原生 QA 扩展到 local→A→B→A→local，使用真实测试认证控制器、空间协调器、正式计划页和临时 PostgreSQL，未提交目标输入隔离及切回恢复 4/4。证据目录 `artifacts/account-ui-android-1790138411775/`。该路径用 QA 控件切换认证身份，尚未验收正式账号页的空间切换提示、离线命令、iOS/iPad 或强退恢复；因此 6.4b 和 SA-05 继续未勾选。详见[Android 草稿空间隔离证据](../../../docs/evidence/backend-auth-mobile-draft-isolation-2026-09-23.md)。

2026-09-23 Android API 36 UIAutomator 经正式 `PlanCreateScreen` 离开确认验证中英“继续编辑”保留输入、“舍弃修改”后重开为空；随后通过正式 `AccountScreen` 登录合成账号并创建本机账号空间，3/3 通过。截图和隔离范围见[Android 计划导航与账号接续证据](../../../docs/evidence/backend-auth-mobile-draft-navigation-2026-09-23.md)。这不覆盖空间切换时的在途编辑保护、离线命令、iOS/iPad 或强退恢复，6.4b 与 SA-05 保持未勾选。

2026-09-23 Android API 36 UIAutomator 通过正式计划页选择“保留输入并返回”，经正式账号页登录/登出验证中文、英文下本机与账号空间草稿分别保留、切换提示显示且新空间不继承旧内容，各语言 1/1。该项补上当前进程内移动端正式空间切换与未提交输入保护，不覆盖服务端撤销恢复、离线命令、iOS/iPad、强杀恢复，因此 6.4b 与 SA-05 仍未整体验收。详见[Android 空间切换草稿证据](../../../docs/evidence/backend-auth-mobile-draft-navigation-2026-09-23.md#64b-增量保留输入并返回--2026-09-23)。

## 7. SA-06 Apple 登录

- [ ] 7.1 严格 login/start/complete 契约、nonce/state/交易秘密、固定 Apple 公钥来源与身份验签，补充负面验证。
- [ ] 7.2 持久一次性流程、授权码交换、身份唯一性、Apple refresh 加密与思玥会话原子完成，恢复和并发验证。
- [ ] 7.3 移动 SDK、真实原生配置、取消与错误、隐藏邮箱及再次登录；真实 bundle/签名/真机验收。
- [ ] 7.4 Apple 相关证据、许可与后续绑定/撤销接口交接；不将 Mock 标为真实接入完成。

7.2 已增加登录准备服务串联与真实PG失败/并发恢复测试，详见 [Apple 证据](../../../docs/evidence/backend-auth-apple-2026-09-22.md)。verified 是上游凭据准备完成，不是思玥登录完成；身份唯一落库及会话原子完成仍缺，因此保持未勾选。

7.2 身份唯一仓储与独立加密Apple凭据表已有本地实现，真实PG并发、命名空间、不可用身份及回滚验证通过。仍需在同一事务串联flow完成、身份、会话与最长60秒最终响应缓存，不能将仓储测试等同公开登录闭环。

7.2 内部登录服务现已将flow完成、身份、会话与60秒加密恢复缓存纳入同一事务，真实PG和本地HTTP并发/回滚/失效测试通过。尚缺公开路由、配置、限流审计、宿主清理调度及真实Apple接入，仍不勾选SA-06整体。

7.2 已接公开start/complete路由、稳定错误、持久限流/审计、受保护配置、启动与定时清理；真实runtime进程及本地HTTP验证通过。默认关闭且无真实Apple配置；仍缺可信反代部署验证、客户端错误接线与真实原生/供应商证据，整体保持未完成。

可信代理CIDR配置和runtime限流接线已补，真实回环HTTP+PG及启动回归通过；仅应用侧证据，尚无Nginx/公网部署验收，见Apple证据的可信代理增量。

7.3 已有共享typed Apple start/complete API和中英错误语义，实际HTTP/PG客户端回归通过；原生SDK、controller授权生命周期、取消/未知结果恢复及UI仍待实现，不勾选。

7.3 共享controller已接Apple授权尝试、取消/代际、同请求重试及安全存储交接，真实HTTP故障恢复测试通过；SDK为合成回调，Expo原生模块和UI尚未接入，保持未勾选。

7.3 后续已接入 Expo `expo-apple-authentication@57.0.2`、iOS 官方按钮、取消/state校验适配器、账号页provider门控及本地原生能力声明；Android/其他平台保持不可用。移动单测141/141与类型检查通过，Apple适配器覆盖3项；Pod安装成功且Apple原生Pod编译通过。模拟器整应用在链接阶段被现有React Native新架构符号缺失阻断，未完成安装/运行；服务端provider仍默认关闭，无Apple Developer签名配置或真实授权。证据见 [Apple 原生接入增量](../../../docs/evidence/backend-auth-apple-2026-09-22.md#expo-原生客户端增量)。因此7.3/SA-06整体继续未勾选。

2026-09-23 进一步采用 Expo 官方 `buildReactNativeFromSource` 构建属性；源码 Pods 成功安装，完整 iOS Simulator Xcode build 成功，iPhone 17 Pro 启动 Metro 后正常显示 Siyue 首页。141 项移动单测、类型检查、Expo config、spec/release/diff 检查通过；截图及构建证据见 Apple 证据“源码构建与模拟器启动复验”。这只证明本地源码构建与 Metro 下启动，不证明离线 Release、Apple Developer 签名、真实 Apple 授权或真机/iPad 验收。provider 仍默认关闭，7.3/SA-06 整体继续未勾选。

## 8. SA-07 设备会话服务

- [x] 8.1 添加严格的设备会话摘要与有界分页合同；仅列当前主体有效会话，不返回认证凭据或完整 IP。
- [x] 8.2 添加本人单设备撤销与全设备撤销接口；撤销其它设备及全部设备分别消费匹配动作的新鲜 reauth grant。
- [x] 8.3 使用真实 PostgreSQL 与 Playwright HTTP 验证分页、跨主体隔离、错误/正确动作授权、重复撤销和最小审计。
- [ ] 8.4 接入中英账号安全 UI 与各平台重新验证流程；Apple-only 重新验证与原生验收。
- [ ] 8.5 完成绑定/解绑、注销编排、派生家庭授权失效和删除作业/回执；真实 Apple revoke 与恢复。

8.1～8.3 以及 8.4 中的桌面/移动界面接线见 [设备会话管理证据](../../../docs/evidence/backend-auth-device-sessions-2026-09-23.md)。桌面中英真实界面已验证列出及撤销其他设备；iPhone 模拟器中英设备列表、本机撤销及英文其他/全部设备撤销、冷启动不恢复通过；Android API 36 模拟器中英其他/全部设备撤销和冷启动不恢复各 1/1 通过。异常凭据误报 503、撤销全部响应丢失与审计行为者标识已增量修复。Apple-only 的服务端 `link-identity` 重新验证已接入合成 Apple 测试，客户端接线、iPad/真机、账号绑定/注销和完整 SA-07 仍未验收，不勾选 8.4 或 SA-07 整体。

8.5 的新增邮箱登录方式服务端切片已按 [Apple 重新验证与邮箱绑定证据](../../../docs/evidence/backend-auth-identity-link-2026-09-24.md) 接入：一次性 Apple reauth grant、会话/凭据版本绑定的验证码挑战、已验证邮箱与密码原子建立，以及同主体跨平台邮箱登录；联合 Playwright HTTP 用例在合成 Apple 与本地邮件适配器上通过。共享 API client 与认证控制器已接线并覆盖丢响应和换号取消；产品账号 UI、桌面受限 IPC、真实供应商、解绑、注销、家庭派生授权与 Apple revoke 均缺证据，所以 8.5 保持未勾选。

2026-09-24 为 8.4/8.5 的 Apple-only 邮箱绑定入口绘制了 [Sage Figma 草稿](../../../docs/design/backend-auth-email-link-figma-2026-09-24.md)，包含手机入口、填写、验证和冲突状态，以及 iPad 英文暗色布局。草稿待维护者评审；它不是已接入的产品界面，不改变 8.4/8.5 的未完成状态。

8.4 的 Apple-only 设备撤销服务端证明已扩展为严格的 `revoke-session` 与 `revoke-all-sessions` 动作，并以 0008 增量迁移约束；共享控制器也已在不暴露 grant 的前提下接入。合成 Apple、临时 PostgreSQL 及真实 HTTP 客户端跨动作/丢响应测试见 [证据](../../../docs/evidence/backend-auth-apple-device-reauth-2026-09-24.md)。正式界面与真实 Apple 授权仍缺验证，8.4 保持未勾选。

Apple-only 家长批准儿童设备的 `approve-child-device` 再次验证已由 0014 增量迁移加入服务端动作白名单，并用合成 Apple、真实本地 HTTP 配对审批和隔离数据库验证错误动作拒绝及一次性消费。共享客户端与移动账号控制器已接入同一预览请求的密码/Apple 再次验证和单次批准；密码路径经 Playwright 真实 HTTP 与儿童领取验证，见[证据](../../../docs/evidence/backend-auth-apple-child-approval-2026-09-24.md)。正式界面和真实 Apple 原生授权仍待接线和验收，8.4/9.3b 不因此完成。

8.5 的已绑定方式展示现在有只读 `GET /v1/me/identities` 来源，仅列当前主体真实可用方式及脱敏邮箱；共享 API client/控制器及 Electron 受限只读 IPC 已按严格合同接线，见 [登录方式摘要证据](../../../docs/evidence/backend-auth-identity-summary-2026-09-24.md)。产品账号 UI、解绑、注销、派生家庭授权和 Apple revoke 仍未完成，8.5 不勾选。

8.5 的邮箱方式安全解绑已接 `DELETE /v1/me/identities/{identityId}`，要求动作绑定的再次验证、最后方式保护、原子撤销及向原邮箱排入安全通知；共享客户端及 Electron 受限 IPC 已接线，见 [邮箱解绑证据](../../../docs/evidence/backend-auth-identity-unlink-2026-09-24.md)。Apple 解绑、产品 UI、注销及真实邮件送达仍未完成，8.5 保持未勾选。

8.5 的注销前置工作已形成严格回执/进度契约、只读本人影响清单与回执状态 HTTP 路由、0015 主库删除作业/回执摘要存储，见 [注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)。单账号可能管理多个家庭，处置请求结构仍待确认；独立防复活账本已有隔离存储原型，但跨存储受理与崩溃恢复尚未接入运行时，真实 Apple 撤销和实际清理未完成。正式注销提交路由仍关闭，不勾选 8.5 或 DEL 验收。

2026-09-24 后续补充：上段是最初检查点；当前已确认逐家庭选择、接收人须为该家庭有效成人且本人明确接受责任、唯一管理者结束管理时冻结仍有其他成员的家庭并立即撤销受影响儿童设备授权、保留共同作品待核对。共享请求合同与当前影响范围的逐家庭精确匹配已接入内部无家庭依赖受理内核，且同键幂等绑定规范化了家庭顺序；尚无接收人接受记录、转交／冻结事务及正式注销提交路由，8.5 仍未完成。定向结果见[注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)。

0020 已为家庭增加 `frozen` 状态；现有家庭查询和儿童会话在该状态拒绝访问，清理器保留冻结家庭关联且不标记服务端数据已删。隔离数据库联合验证 **22/22**。注销受理事务尚未执行冻结、显式 grant 撤销或后续转交处理，因此这项仍未完成。

内部结束管理事务步骤现能冻结唯一 owner 的家庭并同时撤销该家庭儿童 grant、会话、配对申请、邀请和开放房间，失败整体回滚；隔离 PostgreSQL 6/6。该步骤尚未接入完整注销受理，也未处理转交接受或冻结后作品归属，正式提交仍关闭。

0021 新增接收人本人确认记录：有效成人家庭成员可经只读责任范围预览和严格确认 HTTP 路由接受管理与适用监护责任；记录绑定家庭、双方成员及儿童范围，24 小时内可被注销事务重新核对并消费，同状态重复提交返回原记录。内部转交步骤原子调整 owner、监护关系及原管理者儿童授权，内部注销受理已接线全转交选择。隔离 PostgreSQL 及真实 HTTP 定向结果见[注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)。冻结后的可完成处理、混合逐家庭选择、恢复旧备份对家庭转交的重放、正式注销入口及产品 UI 仍未完成，8.5 保持未勾选。

冻结访问边界补查发现内部房间创建只检查家庭存在，现已改为锁定并要求 `active`；席位认领同样检查家庭状态，旧备份遗留的开放房间也不能在冻结家庭内接纳设备。房间容量隔离 PostgreSQL **14/14**、儿童设备真实 HTTP Playwright **3/3**，详见同一证据。该修复不改变 8.5 的未完成状态。

0022 新增冻结家庭待处理记录。内部注销受理现可在同一主库事务中逐家庭混合转交与冻结，冻结时立即撤销该家庭儿童设备授权，按冻结家庭保存待核对记录；清理器保持作业未完成。恢复门禁只在待处理记录与作业、删除中主体及冻结家庭一致时允许其家庭关联留存，孤立监护同意和无记录的残留权限继续拒绝开放。隔离 PostgreSQL 受理／恢复／迁移定向 **39/39**，其后新增冻结事务失败回滚用例，受理单文件 **17/17**；真实进程冻结恢复 Playwright **1/1**，删除客户端 Playwright **6/6**；见[注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)。冻结后的处理人及共同作品核对流程、无其他成员家庭的结束管理、非唯一管理者退出、旧备份恢复一致性与正式注销入口仍待完成，8.5 不勾选。

非 owner 成人结束本人家庭访问的内部步骤已接入注销受理：本人监护的每名儿童须仍有另一位有效监护人，否则整次事务拒绝；家庭及其他成员保持有效。跨家庭并发监护资格检查锁定参与判断的记录，避免旧快照误放行。隔离 PostgreSQL 内核与受理用例及真实 HTTP Playwright 结果见[注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)。上段是此前检查点；此增量已覆盖其中的非 owner 退出，但唯一监护人处置、无其他成员家庭、冻结后处理、旧备份恢复和正式注销入口仍未完成，8.5 保持未勾选。

唯一 owner 的真正空家庭结束管理现由内部受理事务删除家庭及其本人关联；历史成员或任何共享资源记录均拒绝自动删除。清理器删除停用的本人成员关联，且出现新依赖时撤回旧的服务端数据已删标记。独立资料残留审计还发现邀请、监护、儿童设备与房间历史关联尚待确定保留／清理边界，不能据此开放正式提交。定向与全量验证见[注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)，8.5 仍未完成。

资料清理新增历史关联门禁：邀请、监护、儿童设备、配对、房间、解散家庭或冻结处理记录仍指向注销者时，作业保持待处理，不再误报 `serverDataDeleted=true`；受理仍可进行，其他成员的数据不被顺手删除。记录用途与保留期限仍待确认，正式注销入口继续关闭；定向测试与验证边界见[注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)。

后续将已消费的家庭管理接受记录中 owner／接收人主体关联也纳入历史门禁；接收人注销时若记录仍保留，作业继续待处理，不能误报数据已删。隔离清理测试 **22/22** 与真实 HTTP Playwright 定向 **1/1**；见[注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)。

房间邀请 0023 加入后，注销历史门禁也检查邀请人／受邀者关联；记录仍在时不报告服务端数据已删。到期的家庭管理接受记录由既有有界清理作业移除后，删除作业可继续完成。隔离清理测试 **23/23**、房间邀请内核 **14/14**、删除回执真实 HTTP Playwright 定向 **1/1**；见[注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)。房间邀请历史清理政策仍待确定。

内部家庭转交修复跨家庭共用监护同意的误撤回：仅当另一家庭无关系引用时才撤回原管理者的同意；隔离 PostgreSQL 新增红绿回归 **4/4**，邻近定向测试通过，见[注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)。正式注销入口与跨家庭全部处置仍待完成。

真实 HTTP Playwright 定向 **1/1** 进一步验证转交家庭 A 后，另一家庭 B 的儿童会话仍有效、授权和共用同意未被撤回；详见同一[证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)。这不是正式注销提交验收。

冻结家庭时待接受的房间邀请也在同一事务内撤销，真实 HTTP Playwright 冻结路径 **1/1** 核对儿童会话、grant 与邀请状态；见[注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)。冻结后复核未完成。

家庭转交和非 owner 退出同步撤销受影响的待接受房间邀请，保留其他家庭及其他成员仍有效的邀请；事务回滚恢复原状态。隔离 PostgreSQL 联合 **13/13**、真实 HTTP Playwright 定向 **2/2**；具体全量节点及限制见[注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)。正式注销提交、历史清理与冻结后处理仍未完成。

恢复补查：已注销／删除中的主体若仍持有有效家庭管理、成员、监护或儿童设备授权，启动恢复即使看到匹配作业和围栏也拒绝开放。隔离双数据库集成 **15/15**，真实服务进程 Playwright 新增用例 **1/1**。主库受理前备份无法自动重建已完成转交，两库联合回退尚无独立见证；因此正式注销入口继续关闭，8.5 不勾选。

运行时继续核对删除作业、主体状态和家庭权限；不一致时就绪与 `/v1/` 请求保持 503，维护扫描停止，修复后再开放。双数据库恢复集成 **16/16**、服务端全量集成 **394/394**、真实进程恢复 Playwright **10/10**。两库联合回退和家庭处置补全仍未完成，8.5 不勾选。

0016 持久 Apple 撤销队列和 0017 Apple-only `delete-account` 再验证动作已在隔离环境验证；供应商撤销仅用模拟结果，运行时 worker/真实 Apple、删除清理与独立恢复账本仍未闭环，不改变本项未完成状态。

2026-09-24 清理核查修正了邮箱地址复用时的跨主体误删、删除等待期匿名密码重置验证码漏清，以及无会话 Apple 验证流程漏清。注册成功时原验证码绑定主体，过期验证码另设 24 小时清除窗口；隔离数据库回归与验证边界见[注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)。家庭/儿童处置和正式提交仍未完成，8.5 不勾选。

后续内部内核已补齐受理前与清理器一致的依赖阻断、Apple 撤销入队失败回滚及脱敏受理审计；缺失 Apple 凭据时，思玥数据可清理但外部撤销进度保持待处理。独立账本的恢复重放内核只对 `accepted` 标记失效恢复出的登录凭据，`prepared` 保持启动门禁关闭。受理、清理、重放仍未接入正式运行时；已验证的定向范围和缺口见[注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)，8.5 仍不勾选。

内部无家庭依赖受理内核已按账本 `prepare`、主库作业同意图提交、账本 `markAccepted` 的顺序接线，并对确定回滚作 `cancel`；`prepared` 对账内核仅凭匹配的已提交主库作业推进，缺作业保持阻断。v2 账本的实例、水位、条目数及主库 0018 围栏有隔离数据库测试。配置独立账本时，API 启动前会对账、重放并核验围栏，逐次会话操作查询账本；缺作业或未决标记拒绝开放。正式注销提交路由、家庭处置、后台清理和真实供应商仍未闭环，不改变 8.5 状态。

内部有界清理扫描器新增独立账本授权守卫：仅账本 `accepted` 意图与主库作业 ID 一致才可清理，清理事务在行锁内再次核对 ID；双数据库隔离测试覆盖作业 ID 在检查后变化。配置独立账本的真实服务进程现按 30 秒周期调度该扫描器，Playwright 验证已受理作业最终完成。正式注销提交及家庭处置仍待确认，8.5 保持未完成。

运行时账本在启动后新增已受理标记时，维护周期会重新对账、重放并推进围栏；真实服务进程 Playwright 覆盖就绪状态从 503 恢复为 200。Apple 撤销有界运行器已实现隔离测试，运行时接线前仍须校验独立账本授权，避免恢复出的旧队列误调用供应商。

Apple 撤销有界运行器现有独立账本守卫，覆盖队列领取后的供应商调用和批量进度协调；双库集成测试验证未授权不外发且不改回执。长期未授权队首的租约重试可能饿死后续合法队列，运行时调度及真实供应商验收仍关闭。

0019 增量迁移与存储层为未授权领取记录持久重试时间，守卫扫描将拒绝单独计数并继续后续队列，批量协调按游标越过未授权作业。配置 Apple 与独立账本的服务进程现按维护周期调度；真实进程 Playwright 使用本地假供应商验证旧队列零外发、合法队列完成，真实 Apple 与通知未验收。

删除提交响应丢失时，内部受理事务现原子保存短时加密回执与幂等键绑定，恢复读取须先确认独立账本已受理相同作业；预备客户端已要求调用方显式提供 UUID 请求键。隔离 PostgreSQL 与删除相关 Playwright 结果见[注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)。正式提交路由仍关闭，家庭逐项处置与产品 UI 未完成，8.5 保持未勾选。

## 9. SA-08 家庭真实仓储与受控授权

- [x] 9.1 实现家庭、成员关系和可信策略快照的服务端仓储；成人明确创建，读取仅限有效成员。
- [x] 9.2 实现一次性受控家庭邀请，目标邮箱核验、版本检查和并发消费。
- [x] 9.3a 实现服务端监护关系、儿童受限设备配对、授权撤销和实时受限 child session。
- [ ] 9.3b 接入客户端配对与设备管理界面，完成原生设备及跨端操作验收。
- [ ] 9.4 将可信家庭与设备授权接入房间/白板权限，完成跨主体、撤权及多设备负面验收。

9.1 的迁移、仓储、最小 API、默认权限、并发幂等与跨主体拒绝已由 [家庭仓储证据](../../../docs/evidence/backend-auth-family-foundation-2026-09-24.md) 验证；9.2 的一次性邀请、目标邮箱核验、撤权及并发消费由 [邀请证据](../../../docs/evidence/backend-auth-family-invitations-2026-09-24.md) 验证；9.3a 的监护、配对及即时撤权由 [儿童设备证据](../../../docs/evidence/backend-auth-child-devices-2026-09-24.md) 验证。这些勾选只代表本地服务端实现及隔离验收；客户端 9.3b、房间/白板接线 9.4 和 SA-08 整体仍未完成，不将家庭成员身份视为房间、记录或录像授权。

9.3b 的共享客户端接口、家长可信儿童列表与审批前预览已实现并定向验证；移动账号控制器已接入儿童申请、领取及会话安全存储，通过真实 HTTP 冷启动恢复测试。家长只读列表、预览及密码/Apple 批准控制器也已接入；密码批准经 Playwright 真实 HTTP 验证，Apple 授权仍为合成流程。[Figma 交互草稿](../../../docs/design/backend-auth-child-pairing-figma-2026-09-24.md)等待维护者确认页面顺序。正式移动/桌面入口、家长创建界面、原生扫码及跨端实际操作仍未完成，因此 9.3b 不勾选。安全复核与增量迁移 0012 的结果追加在上述儿童设备证据中。

9.4 先落地[五设备席位数据库内核](../../../docs/evidence/backend-auth-room-capacity-2026-09-24.md)：只在内部事务中按真实服务端会话分配最多五席并验证并发；没有房间路由、邀请/家长权限、白板对象授权或 RTC 凭据。该分片没有完成可信家庭与设备授权接线，也不代表真实五设备验收，9.4 保持未勾选。

注销客户端控制器现已接入密码／Apple 再验证、逐家庭预检绑定、原请求显式重试及独立受保护回执；移动存储与桌面 IPC 已接线。共享包与真实 Electron／HTTP 证据见[注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md)。正式 DELETE 路由及完整 UI 仍未开放，真实 Apple 和删除生命周期验收未完成，8.5 保持未勾选。

注销补全新增共享逐家庭流程状态、接收人责任预览／接受控制器和桌面 IPC 接线；实现与验证见[注销前置证据](../../../docs/evidence/backend-auth-deletion-groundwork-2026-09-24.md#注销流程状态与接收人客户端接线)。[iPhone／iPad 流程与冻结、历史处置草案](../../../docs/design/account-deletion-completion-draft-2026-09-24.md)等待维护者具体选择；未启用正式 DELETE、未更改冻结恢复权限或执行历史数据清理，8.5 保持未勾选。

2026-09-25 注销补全：维护者已采纳设计和冻结/历史处置建议；现已接通配置独立账本后的正式 DELETE、桌面/移动页面、成人责任接受、指定运维冻结复核与分类历史清理。上述“等待选择／正式路由关闭／页面未接线”描述保留为历史，不再代表当前实现。当前验证与保留边界统一见[注销补全证据](../../../docs/evidence/backend-auth-deletion-completion-2026-09-25.md)。真实 Apple 与完整删除生命周期验收仍未全部完成，8.5 不作整体完成标记。

## 10. 真实线上邮箱账号闭环（2026-09-25 授权）

- [x] 10.1 发布注册协议版本及双语说明，注册入口、版本校验和失败恢复接线。
- [x] 10.2 复用授权的 Resend 服务，独立队列与密钥配置，验证不确定结果和幂等处理。
- [x] 10.3 独立思玥数据库、注销账本、API 与 worker 部署，公网 TLS 与现有服务回归。
- [x] 10.4 Playwright 验证真实邮件注册、重启恢复、登录和正式注销，记录原生平台覆盖及剩余缺口；见 [2026-09-25 线上及原生证据](../../../docs/evidence/online-account-lifecycle-2026-09-25.md)。
