await figma.setCurrentPageAsync(await figma.getNodeByIdAsync('210:7'));const r=await figma.getNodeByIdAsync('470:17083');r.resize(1440,r.height);const row=await figma.getNodeByIdAsync('470:17087');let ids=[r.id];for(const col of row.children){col.resize(310,col.height);ids.push(col.id);for(const b of col.children)if(b.type==='INSTANCE'){b.resize(270,b.height);ids.push(b.id);}}return{mutatedNodeIds:ids};