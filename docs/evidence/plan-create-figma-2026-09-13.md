# 创建计划入口：Figma 补齐进度

2026-09-13，当前工作树未提交。本次补充现有 Sage 方向内的创建入口，不代表整个 5.3 或账号/家庭功能验收。

## 已完成的设计状态

- [AI 协助 · 中文明色手机](https://www.figma.com/design/5OKY7OMJ2jQy2XAaGbNDGC?node-id=585-34470)：方式切换、目标输入、仅发送本次目标的说明、生成草稿和 AI 服务设置入口。
- [手工创建 · 中文明色手机](https://www.figma.com/design/5OKY7OMJ2jQy2XAaGbNDGC?node-id=586-34488)：同一视觉结构，增加可编辑项目名称，说明下一步添加任务。
- 两个方式按钮已连接到对应状态。AI 主按钮连接既有确认稿，仅为成功路径原型示例；手工主按钮、返回和设置暂未连线，不能宣称完整可点击流程。

复用既有原生输入实例（205:19）、文字按钮（205:160）、Sage 主按钮（548:33775），没有创建第二套按钮或输入组件。容器使用 Auto Layout，颜色沿用已有绑定。新稿沿用现有中文稿 Noto Sans SC 字体；正式应用仍使用系统字体，该差异保留，不声称像素一致。

## 实际检查

先检索代码中的 Code Connect 文件，无对应文件；再用当前 draft 节点读取组件、变量绑定和文字样式（未使用命名文本样式），复用这些来源。两张完整截图均实际查看：选中项、输入、说明和主按钮可见，无首屏截断或重叠。手机逻辑尺寸 390 × 844；手工主按钮底部 652，AI 设置底部 630。节点和组件读回见 [结构记录](../design/plan-create-figma-2026-09-13.json)。

同时用隔离 iPad 的正式应用核查草稿冲突预览：合成草稿 83b184cd-7611-476c-9324-408e28b3ca05 由正式 editDraft 更新为版本2；原生仍持版本1时点击确认，预览按目标、项目、任务三行显示，确认和拒绝禁用。疑似换行转义问题未复现，没有据此修改代码。此操作只创建本地测试草稿，没有保存为正式目标或调用模型。

## 尚未完成

恢复状态的英文、暗色和 iPad 覆盖；专用 AI 服务设置画板及对应连线；生成中的真实进度动效和输入锁定原型表达。基础创建入口现已有手机8种与iPad16种组合，但这不覆盖完整流程或原生验收。5.3d 保持未完成。

Figma 调用经过账号共享锁和账本，顺序执行；本轮5次 use_figma 均成功。历史调用总数仍未知，不据此估算官方剩余额度。没有应用代码改动，未重复应用测试。

## 同日续作：英文、暗色与输入可识别性

新增六个手机稿，连同前两稿共八种创建入口组合：

| 语言 / 主题 | AI 协助 | 手工创建 |
| --- | --- | --- |
| 中文明色 | 585:34470 | 586:34488 |
| 英文明色 | 587:34488 | 587:34515 |
| 中文暗色 | 587:34547 | 587:34574 |
| 英文暗色 | 587:34604 | 587:34631 |

复用既有组件文本属性翻译，英文明色和各暗色稿实际截图已查看；英文说明自然增加一行，末项底部最多676，小于844画板高度。各语言/主题的两种创建方式互相连接；其他原型连线仍按上文待完成，不让克隆稿跳回另一语言/主题。

暗色视觉核查发现主按钮复制了明色显式模式，已将四个暗色主按钮改为547:2，读回和截图均为浅绿背景、深绿文字。嵌套原生分隔线修改模式和画笔后，属性虽改变但截图仍为黑线，未将其计为修复成功；最终隐藏嵌套线，在原输入实例外层绑定controlBorder，使用1单位边框、12圆角，实际截图确认可见。八个稿共12个输入实例均已统一，未分离原组件。最终属性与节点见 [八种组合读回](../design/plan-create-variants-2026-09-13.json)。

正式应用 `plan-create-screen.tsx` 同步为controlBorder、1单位边框、12圆角和16水平内距；只改创建页输入外观，不改审批、数据或执行语义。隔离iPad（同上设备，英文暗色横屏）实际进入手工创建，输入 `Improve communication at work` / `Work communication practice`，观察边框完整、文字有内距、主按钮由禁用转为可用。没有提交或外发。此证据不覆盖手机键盘、其他应用主题/语言或真机。

续作执行 `corepack pnpm --filter @siyue/mobile typecheck` 通过，`git diff --check` 通过。局部样式未新增镜像实现的测试。Figma续作12次调用均成功、无429，仍使用共享锁、8秒间隔与账本；嵌套线的渲染问题按截图证据另行处理。整体5.3仍未完成。

## 同日续作：iPad 英文手工创建

| 方向 / 主题 | 节点 | 逻辑尺寸 |
| --- | --- | --- |
| 竖屏明色 | 591:34542 | 834 × 1194 |
| 横屏明色 | 591:34573 | 1194 × 834 |
| 竖屏暗色 | 591:34604 | 834 × 1194 |
| 横屏暗色 | 591:34635 | 1194 × 834 |

表单内容保持632宽（对应应用680最大内容宽减去左右24内距），横竖屏居中；导航单独一行靠左，不跟随表单居中。输入实例、主题绑定和字段内距沿用上一轮。四张截图实际查看，英文说明完整，主按钮底部652，横屏834高度可容纳。暗色主按钮显式使用暗色模式，无上一轮复制明色按钮的问题。

本次仅补Figma设计，四个稿的动作连线尚未设置；返回暂为文字，仍需与应用图标一致。没有将代表性应用iPad截图当作这些新设计的逐像素验收。节点、结构和几何读回见 [iPad设计记录](../design/plan-create-ipad-2026-09-13.json)。本轮4次Figma调用成功，遵守共享锁与间隔；无应用代码修改，不重复应用测试。中文与AI方式继续待补。

## 同日续作：iPad 基础入口组合补齐

新增12稿，现iPad基础入口覆盖两语言、两主题、两方向和AI/手工两种方式：

| 语言 / 主题 / 方向 | 手工创建 | AI 协助 |
| --- | --- | --- |
| 中文明色竖屏 | 592:34582 | 592:34823 |
| 中文明色横屏 | 592:34613 | 592:34851 |
| 中文暗色竖屏 | 592:34644 | 592:34879 |
| 中文暗色横屏 | 592:34675 | 592:34907 |
| 英文明色竖屏 | 591:34542 | 592:34706 |
| 英文明色横屏 | 591:34573 | 592:34739 |
| 英文暗色竖屏 | 591:34604 | 592:34767 |
| 英文暗色横屏 | 591:34635 | 592:34795 |

新增稿截图均实际查看，保持632宽内容、独立导航和语义变量。中文发送说明在iPad为一行、英文为两行，未裁切；两种方式的项目字段显示规则保持不变。16个方式切换按钮已设置相互导航，读回目标均在同语言/主题/方向的一对稿之间，不跳到手机或其他主题。此为节点连线读回，不等同浏览器中逐一点击验证。

结构、完整新增节点和连线读回见 [iPad矩阵记录](../design/plan-create-ipad-matrix-2026-09-13.json)。本轮8次Figma调用成功；未改应用代码，不重复应用测试。其余动作、异常状态和返回图标仍按上方未完成清单继续，未把5.3改成完成。

## 同日续作：恢复状态首批稿

新增中文明色手机代表状态，复用原输入与按钮实例：

| 状态 | 节点 | 表现 |
| --- | --- | --- |
| AI生成失败 | 593:34686 | 保留目标，错误说明使用error语义色，保留重试、手工方式和设置入口 |
| 手工保存结果未知 | 593:34714 | 保留目标/项目，说明输入已锁定，主操作为核对并恢复同一份草稿 |
| 正在生成 | 593:34768 | 显示生成中，主操作为停止，设置入口变淡 |
| 已停止 | 593:34745 | 明确说明目标输入保留，可重新生成或切手工方式 |

四张截图实际查看，最长的未知结果页末项底部720，位于844高画板内；未见提示裁切。失败/已停止页的生成按钮连接生成中，停止按钮连接已停止，两页的手工方式连接既有中文明色手工创建。属性读回见 [恢复状态记录](../design/plan-create-recovery-2026-09-13.json)。这是静态状态及连线证据，没有模拟服务返回或原型中逐一实点验收。

尚未完成：这些状态的英文、暗色和iPad覆盖；生成完成/未知结果对账后的连线；输入锁定的原型交互表达；生成中的原生进度指示；返回和设置目的地。失败提示色的应用同步见下方续作；没有将静态提示当作实际权限或幂等证明。

本轮4次Figma调用成功，使用共享锁和账本；未改应用代码，未重复应用测试。`git diff --check`通过。

## 同日续作：应用提示同步与配置缺失回退

`plan-create-screen.tsx` 的失败提示改用error语义色；主动停止、手工保存结果待核实和需要配置服务三类提示仍用ink，不将取消或待核实结果误标为失败。`messages.ts` 中英两套无法读取/无法整理计划的提示去掉JSON、字段结构等内部术语；错误分类和受控错误映射保持不变，不显示供应商响应。

执行 `corepack pnpm --filter @siyue/mobile typecheck` 通过；`corepack pnpm --filter @siyue/mobile exec node --experimental-strip-types --test tests/i18n.test.mjs tests/plan-error.test.mjs` 5/5通过，覆盖双语键及插值一致性、受控错误映射和不泄露未知错误。存在Node既有MODULE_TYPELESS_PACKAGE_JSON警告，未为本次修改包配置。

隔离iPad当前应用（英文暗色横屏）输入合成目标 `QA configuration recovery` 后点击生成，显示Configure an AI service in Settings, or create a draft manually，目标保留、提示为中性色。点击Create manually，目标继续保留，配置提示消失，项目字段出现，项目为空时创建按钮禁用。没有配置供应商或发出模型请求：当前usePlanGeneration在配置缺失处直接返回configuration_required。未创建草稿或正式计划。

此次原生检查覆盖配置缺失与手工回退，不覆盖网络失败红色、停止或结果未知状态的原生画面；这些仍需对应故障验证。中文新提示经资源测试，未在本步重测中文原生画面。`git diff --check`通过。

## 同日续作：创建页返回控件统一

28张既有创建/恢复画板的返回实例统一复用Simple Design System的Icon Button（Subtle / Default / Medium）及Chevron left矢量组件；点击区域48×48，矢量颜色绑定Siyue ink，iPad独立导航容器同步为48高。保留原实例ID，未新增手绘组件、应用依赖或修改业务关系。完整受影响节点和尺寸见 [返回控件记录](../design/plan-create-back-controls-2026-09-13.json)。

旧iOS符号组件因缺失字体无法设置Symbol，改用已有矢量组件；首次使用跨调用缓存的导入variant ID得到null，失败调用无写入，修正为在当前调用中按组件key导入后使用返回节点，随后成功。不把字体或节点错误认定为限流。所有调用按共享锁、账本和8秒间隔执行。

实际查看中文明色AI手机、英文暗色手工手机、英文暗色横屏iPad、中文明色未知结果手机截图：返回图标在左上导航区域，暗色图标可见，内容无重叠或裁切；28张结构读回均为48×48，最长手机内容底部724，小于844画板高。内部Chevron实例沿用组件布局为20×20。截图抽查不等于28张逐一交互验收。

此次未改应用代码，不重复应用测试。返回目的地及其他前述未完成交互仍需补齐，本步只完成控件和布局统一，不宣称完整创建流程或版本验收。确认的目标→可改名项目→任务关系已在docs/decisions.md的2026-09-09记录中保留，不重复新增决策。

## 同日续作：创建、返回与恢复路由

创建入口的原型路由已按真实应用语义补齐：普通创建、失败、停止及手工结果未知画板的27个返回控件使用 Figma `BACK` 动作，保留进入页面前的来源；生成中画板不响应返回。手机与 iPad 的 AI/手工主操作共24个，以及手工结果未知的核对成功操作，共25条成功路由均进入同语言、主题、设备和方向的草稿确认画板，不跳过确认直达保存结果。

现有 Figma 文件没有独立 AI 服务设置画板。15个相关入口保持无点击连线，没有误接到通用设置；该缺口随专用设置画板一并补齐。最终程序化读回验证27个返回动作、25条成功路由、生成中禁用返回和15个未误连设置入口，失败数为0。目标矩阵和核查结果见[路由核查记录](../design/plan-create-route-audit-2026-09-13.json)。

本轮 Figma 调用继续使用账号级共享锁、账本、单请求和至少8秒的起始间隔；调用均成功，无429。此次只修改 Figma 原型并更新证据，没有改应用代码，也没有用静态连线替代原生业务验收。

## 同日续作：AI 服务设置手机基线与代码语义

在 `5.3 · AI 服务设置` 分区建立中文/英文 × 明色/暗色四张手机未配置基线，并为每张建立同语言、同主题的供应商选择器。页面复用现有返回按钮、Tall Row、主题卡片和披露图标；选择器包含搜索以及 DeepSeek、通义千问、OpenAI、Anthropic 和自定义服务五个入口。四张未配置页的供应商行均连接对应选择器。

创建流程已有六个可操作的手机 AI 服务入口已连接到对应基线：四张基础创建页，以及中文明色的失败、已停止恢复页。生成中页面仍不提供可操作设置入口。iPad 入口继续保持未连线，等待八张 iPad 专用基线，避免跨设备或方向跳转。节点、路由、已验证项和剩余项见 [AI 服务设置手机记录](../design/ai-provider-figma-phone-2026-09-13.json)。

实际视觉检查覆盖修复后的完整分区、中文明色未配置页和中文明色供应商选择器；随后在已登录 Chrome 中重新打开目标 Figma 节点，确认文件、页面列表、分区和画布正常加载。该浏览器检查证明设计文件当前可访问，不替代逐条原型点击或原生设备验收。

配置完成页的两次创建尝试均原子回滚：第一次为虚拟子节点引用无效，修正后第二次因字体未加载失败。按普通失败最多重试一次的调用规则，本轮停止重复写入，没有把失败结果记为已创建，也没有把它误判为限流。两次均记录到账户共享账本；本轮未出现429。

应用同步修正两项设计一致性：隐私说明现在明确区分对话发送当前会话文字、计划生成发送本次目标与固定格式说明；状态反馈新增 info/success/error 图标和文字语义，错误具备 alert 角色，避免只依赖颜色。执行移动 TypeScript 检查通过；`ai-provider-status`、双语资源和主题对比的定向 Node 测试共11项通过。

仍未完成：八张 iPad 基线、配置表单、模型获取与选择、保存成功、可恢复错误、未保存离开保护等代表状态，以及原型实点和原生设备矩阵。因此 5.3d 继续保持未完成。
