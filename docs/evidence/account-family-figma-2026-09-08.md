# Figma 账号与家庭空间草稿 · 2026-09-08

状态：部分草稿，未完成设计评审，未进入正式 UI 实施。

## 订阅恢复与 R3 核心页面精修

2026-09-08 用户自行订阅后，whoami 确认 Professional / Full，原文件恢复读写。以下历史限额记录已解除，不再作为当前阻塞。

维护者检查后指出初稿“全部都是文字”“不像一个产品”，并要求补图标与色彩。R2 加入导航图标、头像、进度、任务行后，维护者反馈“确实改善了很多”，进一步要求设计感与高级感。R3 已细化蓝灰焦点卡、语义进度、卡片边界、字体与间距；具体配色尚待评审。

| 当前评审画面 | 节点 |
|---|---|
| 我的空间 R3 | 66:147 |
| 家庭空间 R3 | 66:194 |
| 目标详情 R3 | 66:240 |
| iPad 家庭三栏 R3（1180×820） | 94:2 |
| 完成一项任务 | 94:87 |
| 首页更新为3/5 | 106:2 |

[从 R3 首页查看](https://www.figma.com/design/5OKY7OMJ2jQy2XAaGbNDGC?node-id=66-147)。旧稿和 R2 保留对照；全部画板及 R3 图层、连接的真实 ID 见 [R3 清单](../design/account-family-figma-r3.json)。画板数量不代表完整交付。

验证：实际 screenshot 查看三张手机整屏与 iPad 整屏；图层边界检查发现家庭摘要高度和添加任务文本宽度问题，修复后六个核心画面/状态的文本边界溢出为0。已回读5条 prototype reaction：首页→目标→完成状态→更新后的首页，以及对应返回入口。仅验证连接结构，尚未用浏览器逐项点击验收；演示中的“已同步”为合成状态，不是云端请求证据。新任务、语言切换及家庭完整流程尚未连通。

图标来自 Lucide 上游 SVG（读取13个，未下载成功的4个未使用），未将文字字符冒充系统图标。系统组件库导入在 Professional 下仍返回 Not permitted to upsert，保持可编辑草稿图层；尚未组件化为正式设计系统。中文使用已验证可用的 Noto Sans SC Regular/Medium，不能视为原生系统字体验收。

未完成：R3 全流程扩展、中英/明暗矩阵、iPad竖屏与窄窗口、完整点击与无障碍验收、正式 UI 实施。未修改应用代码、发送邮件邀请或部署服务。

本地文档验证（工作目录 `/Users/feature/code/siyue`）：`corepack pnpm spec:check` 4项通过；`corepack pnpm release:check` 通过；`git diff --check` 通过。未执行应用构建、模拟器或真机测试，本轮未修改应用实现；未 commit/push。

## 早期流程草稿记录（历史）

### 后续补充：设置与语言切换实际点击

新增 `150:2` 空间导航、`150:3` 中文设置、`150:4` 中文语言选择、`150:5` 英文设置、`150:6` 英文语言选择、`150:7` 外观选择稿。设置使用账号摘要与分组行，独立语言入口；只有简体中文/English、明色/暗色。已核对现有 i18n/theme 代码，不增加“跟随系统”第三外观选项，不要求额外保存。

在 Chrome 的真实 Figma prototype 中按可见控件点击，URL节点和截图依次验证：`150:3 → 150:4 → 150:6 → 150:5 → 150:6 → 150:4 → 150:3`。验证包含切换 English、英文选中态、返回英文 Settings、再切中文及返回中文设置。浏览器中的英文 Devices & sessions 换行可见，Figma整屏文本边界检查无溢出。浏览器视口较矮时下方设置不在首屏，本轮没有据此宣称所有底部入口可达。

[可点击设置原型](https://www.figma.com/proto/5OKY7OMJ2jQy2XAaGbNDGC?node-id=150-3&starting-point-node-id=150%3A3)，[节点与连接清单](../design/account-family-figma-r3-settings.json)。这是静态画板状态切换，不能证明语言本机持久化或业务后端已实施。

剩余：外观的暗色选中/返回状态尚未连通，英文设置返回主导航尚待完整本地化；同步、AI服务、数据与设备入口未完成详情交互；设置 iPad 与完整主题矩阵未验收。未修改应用代码、账号、系统语言或真实服务配置。

### 后续补充：英文、暗色与 iPad 竖屏

- 英文目标明色 `112:2`、英文目标暗色 `117:2`：两行标题、英文任务和状态文字已在整屏 screenshot 中看到，文本边界检查为0溢出。
- iPad 竖屏 `136:2`（820×1180）：双栏目标列表/详情，折叠侧栏后保留48×48导航入口；进度宽度按新容器重新计算为1/3，整屏 screenshot 与边界检查通过。横屏仍为三栏。这仅覆盖当前家庭目标示例，不代表完整平板尺寸矩阵。
- SF Pro 虽可 load/read，但首次截图仍保留克隆中文，创建新 SF Pro 文本还出现不可见渲染；切换 Noto Sans SC 并重新排版后截图才出现英文。因此所有这些英文评审稿使用 Noto Sans SC，原生系统字体验收保持未完成。未把最初字体/字符元数据当作视觉通过。
- 对比度计算发现候选 `#547B67` / `#F2F6F3` 为4.3695:1，不足普通文本4.5:1；绿色状态文字改为 `#476B58`，该组合为5.4819:1。另5组明色文字组合均超过4.5:1。仅覆盖抽样不透明色对，不代表完整无障碍通过。
- 文件已整理：`0:1` 当前核心评审9画板，`145:2` 早期流程草稿39画板，`145:3` R2对照3画板；未删除旧稿。新增6个原型评审起点。ID、字体、检查结果见 [适配清单](../design/account-family-figma-r3-variants.json)。图层位置和页面归属以此清单为准，R3早期快照保留历史记录。

新增稿仍未覆盖全流程英文/暗色、窄窗口、大字体及逐项浏览器点击验收；不因此勾选 tasks 2.1/2.2。

[真实 Figma 文件](https://www.figma.com/design/5OKY7OMJ2jQy2XAaGbNDGC)，位于当前唯一团队 drafts。用户已明确同意提案交互方案并继续，未改变公开权限或发布文件。

## 已创建

| 画板 | node ID |
|---|---|
| 登录 | 3:2 |
| 邮箱验证码 | 3:3 |
| 我的空间 | 3:4 |
| 家庭空间 | 3:5 |
| 邀请家人 | 3:6 |
| 接受邀请 | 3:7 |
| 创建儿童身份 | 3:8 |
| 儿童设备配对 | 3:9 |

8 张 390×844 手机画板，共72个内容图层，使用自动布局与可编辑文字。仅合成数据，无实际邮件、邀请码、儿童信息或后端动作。按钮尚未设置 prototype reactions；点击不代表真实业务。样例验证码/配对码仅视觉占位。

项目没有 Code Connect；空文件无已有画板/本地变量/样式。查询到 iOS 社区库，但导入被 Figma 拒绝（Not permitted to upsert from library）；随后核对 whoami 为 Full seat、Starter plan。改用项目灰阶/间距的普通可编辑草稿图层，未生成正式组件库。字体库提供 Noto Sans SC，未返回 PingFang SC；中文草稿用 Noto Sans SC，正式原生系统字体约束不变。

## 验证及阻塞

首次 login screenshot 发现文字固定40高导致说明裁切；已修复自动高度。修复后返回的文字高度和内容底部通过几何检查，手机各画板内容底部512–568，小于844；这不代替视觉验收。

绘制8张画板后，截图复验请求返回“reached the Figma MCP tool call limit on the Starter plan”。再次 whoami 核实仍为 Starter；未自动升级、购买或重复请求消耗限额。最终图形复验、iPad、共享/冲突/撤权状态及可点击跳转均未完成。

续作清单与已创建按钮ID见 [画板数据](../design/account-family-figma-draft.json)。限额恢复后直接续作同一文件，不再创建重复文件。需先截图检查现有画板，再补余下草稿和导航。8张手机画板不代表中文/英文、iPhone/iPad全范围交付。

本轮仅规格确认与部分设计稿。未修改应用代码、依赖或数据；未commit/push、生产部署或发送邀请。

## 2026-09-09 · 视觉方向确认与设置补充

维护者确认 R3 作为后续页面视觉基准。新增暗色外观选择 `183:2`、暗色设置 `183:19`、iPad 中文设置 `192:6` 与英文设置 `196:31`。英文设置已补齐界面翻译并修正 English 选中标记，实际 Figma 截图检查通过。暗色设置的语言及返回导航尚未完整连接；本轮不声称主题切换浏览器往返已验证。此前语言切换浏览器证据仍独立有效。

本轮更新设计决策与草稿，不修改应用代码；未执行原生、真机或生产同步测试。
