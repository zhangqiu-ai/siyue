# SA-07 注销前置能力与边界 · 2026-09-24

工作树为 `feature/dev`，保留既有未提交改动。本批未操作生产数据、未开放删除接口、未提交或推送。

共享契约先定义 `DELETE /me/account` 的显式确认与一次性授权请求、202 受限回执及回执经请求体查询的最小进度响应。`dependencyDisposition` 的具体组织方式仍待维护者确认：一个账号可管理多个家庭，当前单个选择不能代表不同家庭的不同处置。因此该请求契约尚未接入服务端，不能视为已定产品行为。

服务端新增只读注销影响清单：在同一数据库事务内验证并锁定当前成人会话，再读取本人有效家庭成员关系、明确的监护关系及本人仍有效的儿童设备授权；家庭角色不自动产生监护关系。只返回本人和家庭／儿童 ID、角色与人数，不返回其他成人 ID、姓名、邮箱或凭据。`GET /v1/me/account/deletion/impact` 现已注册，要求唯一的 Bearer 授权头；它不执行注销或改变家庭授权。

隔离临时 PostgreSQL 集成测试覆盖空依赖、本人/他人隔离、唯一与共同监护、失效同意和关系、凭据漂移、受限儿童会话拒绝、有效设备授权计数及只读行为。下方记录本次最新检查；先前检查属于前一代码状态。

本检查点的完整注销仍缺独立于主库备份的防复活账本、家庭/儿童处置决定、Apple 外部撤销与失败恢复、实际个人云内容清理和正式客户端入口。现有主库表的外键也不允许简单硬删主体。故 DEL-01～07、SA-07 和 8.5 均保持未完成，当前代码不得作为“账号已可注销”展示。

## 增量：作业与回执存储

0015 以增量迁移建立主库 `account_deletion_jobs`，每个主体至多一项作业，只保存 32 字节高熵回执的 SHA-256 摘要、到期和两个独立清理维度；`completed` 状态必须有完成时间，且服务端数据已清理、外部撤销不再待处理。主库运行账号仍可修改此作业表，它**不是**防复活账本。独立账本及恢复前重放机制均未实现，因此正式注销受理继续关闭。

内部作业仓储仅允许已进入 `deletion_pending` 的主体创建作业，回执原文只在创建结果中出现；未知、错误和过期回执统一拒绝。状态读取只返回最小进度，公开合同使用 `serverDataDeleted` 明确指思玥服务端管理的数据，不宣称手机或其他离线副本被清理。作业创建和主体状态变更必须处于同一主库事务；独立账本需要跨存储崩溃恢复流程，不能把两次写入称作一个数据库事务。

隔离临时 PostgreSQL 的 0014→0015 升级、约束、权限与回执仓储定向测试 **10/10**；共享契约 **55/55**，相关构建通过。`POST /v1/account/deletion/status` 已注册，只接收请求体中的回执，不接收 Bearer 或查询串；错误回执不暴露主体资料。`DELETE /v1/me/account` 仍返回 404，因为注销受理、客户端入口、Apple 撤销调用与实际删除尚未完成，DEL 与 SA-07 状态不变。

前一检查点：服务端全量集成 **210/210**（含 HTTP 只读路径及删除路径关闭检查）、全仓类型检查 **13/13**、`spec:check` **6/6**、`release:check`、`git diff --check` 通过。后续增量的全量检查需另记。没有 Apple 实际接口或真机验收。Apple 当前官方说明要求应用内可发起完整账号删除；可要求再次验证与确认，但不得把停用账号当删除；使用 Sign in with Apple 时应调用撤销接口，缺少可撤销令牌也不能拒绝删除请求。来源：[账号删除指导](https://developer.apple.com/support/offering-account-deletion-in-your-app)、[Apple 登录删除与撤销](https://developer.apple.com/documentation/technotes/tn3194-handling-account-deletions-and-revoking-tokens-for-sign-in-with-apple)。

内部 `account-deletion-accept` 原型仅处理无任何有效家庭/监护、儿童设备或待处理邀请的成人：同一主库事务要求 `delete-account` 再次验证、将主体置于删除中、撤销所有本人会话与刷新链、取消待发送验证邮件并创建回执作业。隔离数据库 6 项定向测试覆盖会话即时失效、家庭依赖拒绝、作业插入失败整体回滚、Apple 凭据缺失时不阻止本人删除、持久撤销任务同事务提交及邮件取消。它没有 HTTP 入口，不执行实际资料清理；并发家庭邀请接受与依赖读之间的序列化规则、跨存储防复活与清理仍待闭环，不可直接开放。

Apple-only 账号的 `delete-account` 再验证动作已由共享契约和 0017 增量迁移允许；合成 Apple 事务与 0016→0017 升级测试 **15/15** 通过。0016 增量迁移新增独立的持久 Apple 撤销队列表，工作凭据只存沿用原加密密文；适配器单次调用官方固定撤销端点，状态机负责有限重试与超时，持久仓储使用物化候选的 `FOR UPDATE SKIP LOCKED` 与租约栅栏，避免并发领取及过期请求覆盖结果。模拟供应商定向测试 **14/14**、隔离数据库持久仓储与 0015→0016 升级测试 **9/9** 通过，没有真实 Apple 请求。该队列尚未由正式受理路由或运行时 worker 调度，完成时间、错误通知和真实撤销均不作已验收声明。

共享影响清单 schema 已移入 contracts，客户端有严格校验的影响清单、回执查询和预备提交方法。真实 Fastify＋临时 PostgreSQL 的 Playwright 测试 **3/3**：本人影响清单与失效会话、受限回执进度、提交路由保持关闭。预备提交方法不等于用户可见注销入口。

本轮增量修复了缺失 Apple 撤销凭据的进度误报：清理器现在允许删除思玥服务端资料，但保持 `providerRevocationPending=true` 与 `apple_credential_missing`，后续协调也不得据已移除的身份行误判为 Apple 已撤销。服务端构建及隔离 PostgreSQL 清理测试 **16/16**、真实 HTTP 回执 Playwright **4/4** 通过。正式提交路由、独立账本运行时接线、家庭处置和真实 Apple 撤销仍未验收。

后续同轮增量：受理内核的 Apple 撤销队列改为必需依赖，入队失败整体回滚；`dependencyDisposition:{kind:'none'}` 明确校验，并在受理前复用清理器的完整依赖检查，防止漏掉孤立监护同意、配对或开放房间。清理完成时清空旧错误码。只读影响清单与回执路由共用四个并发槽，超额返回可重试 429。独立账本增加恢复重放内核：仅对 `accepted` 失效恢复出的凭据并写回删除状态；对 `prepared` 不执行删除且保持启动门禁关闭，缺失或矛盾资料也失败关闭。该内核仍未接入启动或登录路径，也不清除恢复出的个人资料，因此不代表防复活链路已验收。

定向验证：`corepack pnpm --filter @siyue/server build` 通过；`node --test` 对受理、清理、重放三个隔离 PostgreSQL 文件在门禁微调前 **30/30**，门禁微调后重放文件 **6/6**；`corepack pnpm exec playwright test tests/e2e/deletion-client.spec.mjs` **5/5**。首次 Playwright 因根目录无法解析 server 的 Fastify 依赖而未收集用例，改为从 server 包解析后同一用例通过。全仓 `typecheck` **13/13**、`spec:check` **6/6**、`release:check` 通过。正式注销提交仍为 404；没有真实 Apple 调用、运行时恢复演练或真机验收。

下一轮增量：受理事务新增不含邮箱或正文的 `account.deletion.accept` 安全事件，作业失败时审计同事务回滚；受理定向测试 **8/8**。新增有界作业清理扫描器，已修正等待 Apple 撤销的旧作业占满扫描批次的问题：新受理优先、等待撤销按 30 秒节流，隔离 PostgreSQL **9/9**；真实 HTTP 回执 Playwright **5/5** 再次通过（测试通过扫描器推进服务端清理）。共享客户端保留 `deletion_dependencies` 与 `adult_required` 两种明确拒绝，契约测试 **56/56**、适配器测试 **169/169**。主库新增仅含账本实例与序号的空围栏表，0018 增量迁移及围栏读写／回退检测隔离测试 **2/2**。这些代码仍未使正式注销受理或后台调度上线。

账本存储增量：独立账本 v2 现有持久实例 UUID、单调序号和条目数，主库围栏可区分同实例推进、账本回退和换库。事务转换在写入前校验完整性，防止截断后通过新写入掩盖水位差异；读取同时检查序号与条目数，能发现删去非最新记录的情况。临时双数据库账本与恢复内核定向测试 **21/21**，围栏迁移测试另 **2/2**；仅为隔离存储验证，尚无运行时连接、启动重放、逐次登录门禁或 v1 账本升级。账本与主库同时回退的识别仍需独立见证。

内部受理内核现要求账本依赖：先以已验证会话识别主体，主库事务内再次锁定并验证会话、依赖和一次性授权；在主库提交前 `prepare`，将相同意图 UUID 写为删除作业 ID；确定回滚才 `cancel`，提交成功后才 `markAccepted`。若主库提交后账本写入失败，保留 `prepared` 和已提交作业供后续对账，当前调用不会返回成功回执。临时双数据库受理测试 **9/9**，包含回滚补偿与提交后标记失败；尚未接入正式路由或恢复调度，不能视为完整注销能力。

`prepared` 对账内核只在主库同意图作业和删除中／已删除成人主体一致时推进账本，缺作业、矛盾或读取失败均留待处理；隔离双数据库定向 **9/9**。受理后标记失败的联动测试现证实可由该内核对账。首次账本 `prepare` 的并发重试以主体级事务锁序列化，避免重复水位。受理、账本、对账及 0017→0018 迁移相关测试联合 **49/49**；全量服务端集成尚待本轮所有改动完成后复跑。

启动恢复协调内核在接线前的隔离双数据库检查为 **12/12**：空账本先重放再绑定、旧主库备份重放已受理主体、未决 `prepared` 与不一致记录拒绝开放、账本换库／回退拒绝，以及重放期间或围栏写入期间账本前进的竞态。独立账本运行配置校验 **8/8**，缺一项即报配置错误。该检查点的内核与配置尚未在 `index.ts` 接线，不能把内核测试等同于运行时启动保护或真实恢复演练。

运行时接线增量：独立账本配置齐全时，真实服务进程先对账、重放并核验主库围栏，失败即不监听；会话签发、验证、刷新逐次核对账本，账本暂不可用时 `/v1/account/session` 返回 503，`/health/ready` 同时拒绝，`/health/live` 仍可读。恢复若缺少已受理作业，虽会撤销恢复出的凭据并阻断复活，仍拒绝推进围栏和开放服务；有作业但资料未清理时保持 `deletion_pending`，不把受理标记误称为已删除。真实进程、临时主库与独立账本的 Playwright **5/5**，启动恢复内核 **13/13**，会话门禁隔离测试 **8/8**，服务端单测 **43/43**，服务端全量集成在最后一次受理顺序调整前 **315/315**；调整后受理定向 **10/10**。`spec:check` **6/6**、`git diff --check` 通过。正式注销提交与后台清理调度仍关闭，没有生产恢复演练或真实 Apple 撤销。

账本授权清理增量：有界扫描器现有独立账本守卫，仅对 `accepted` 且意图 UUID 等于主库作业 ID 的主体调用清理内核；`prepared`、`cancelled`、缺条目、意图不符或账本不可用均不清理。守卫读完账本后，清理事务在作业行锁内复核相同 ID，避免主库作业被替换的竞态。临时双数据库集成 **11/11**，含两次读取之间作业 ID 改变的拒绝用例；接入运行时前服务端全量集成 **327/327**、删除相关真实进程 Playwright **10/10**、全仓类型检查 **13/13**、`spec:check` **6/6**、`git diff --check` 通过。

运行时清理接线：配置独立账本时，服务进程在既有 30 秒维护周期内运行授权扫描器；真实进程 Playwright **5/5**，其中一例从已受理作业启动并等待定时扫描，确认匹配账本标记的主库作业完成、主体转入已删除状态，同批缺少账本标记的主库作业保持原状。此检查不包含正式注销提交路由、Apple 真实撤销或家庭处置；不能称账号注销已可用。

运行期间账本水位变化的恢复已接入同一维护周期：只有重新对账、重放并推进围栏成功才开始该轮清理；真实服务进程 Playwright **7/7**，新增用例验证服务就绪先变 503，维护周期后恢复 200、对应作业清理完成。另一用例验证账本实例被替换时，启动在修改 `prepared` 标记前拒绝。Apple 撤销有界运行器的隔离 PostgreSQL 测试 **9/9**，但它尚未接入服务进程或真实供应商；恢复出的旧队列必须先受独立账本授权约束，不能直接调度。

受理顺序再收紧：先在主库事务内查完家庭与监护依赖、消耗一次性再验证授权，再写独立账本 `prepared`；依赖拒绝不会留下无意义的 `cancelled` 标记，也不会让服务就绪状态因账本水位变化而短暂下降。Apple 入队失败仍在已 `prepared` 后回滚并取消意图，提交后的未知结果仍留待对账。受理定向集成 **10/10**。

Apple 撤销运行器增加独立账本守卫，分别约束队列领取后的外发和批量回执进度更新：只有 `accepted` 且意图与主库作业 ID 匹配才可执行；缺失、未决、撤回、错意图、账本不可用或不可读均不调用模拟供应商，未授权作业的 `provider_revocation_pending` 与错误码也保持原状。临时双数据库守卫集成 **17/17**；本轮服务端全量集成 **353/353**。守卫尚未接入真实运行时。现有领取方式在拒绝后保留 60 秒租约，长期未授权的队首可能反复阻断后续合法撤销；须解决调度饥饿并验证恢复后再接真实 Apple 适配器。

0019 增量迁移增加与撤销截止时间分开的 `authorization_retry_at`：未授权队列领取会在保留密文和原可用时间的前提下回到 `pending`，五分钟后再试；到点前领下一行，拒绝与供应商尝试分开计数。批量进度协调以游标越过未授权作业，避免它们反复占住同一批次。存储层、守卫和运行器联合隔离 PostgreSQL **41/41**。配置 Apple 与独立账本的服务进程已接上队列；真实进程 Playwright 使用进程内拦截的本地假 Apple 响应，验证旧队列零外发且回执不变，后续授权队列恰好一次模拟撤销并完成删除。本次服务端全量集成 **359/359**、删除相关 Playwright **13/13**、全仓类型检查 **13/13**、`spec:check` **6/6**、`release:check` 与 `git diff --check` 通过。此结果不证明真实 Apple 接口、供应商凭据或用户通知。

## 删除提交响应丢失的恢复切片

设计 14.5 要求注销提交携带 `Idempotency-Key`，此前预备客户端却没有发送；且主库删除事务提交后若账本 `markAccepted` 失败，用户会话已经失效，作业只保留回执 secret 摘要，用户无法再取得进度证明。本切片新增主库事务内的幂等记录：键、原 bearer、一次性 grant 与完整请求仅以服务端 HMAC 绑定；回执加密保留 60 秒，操作元数据保留 24 小时，`subject_id` 留空，避免删除清理抹掉短时恢复记录。读取相同请求时先核对独立账本是否 `accepted` 且意图等于作业 ID；未确认返回可重试 503，确认后才恢复同一回执，过窗返回专用 409，不重新创建作业。预备 API 客户端现在要求调用方提供并复用 UUID 请求键，不自动生成或重试。正式 HTTP 删除入口仍未开放，客户端尚无可见注销流程。

隔离 PostgreSQL 回执存储测试 **9/9**，涵盖会话撤销后的同键读取、不同凭据冲突、并发同键唯一提交、60 秒过窗、24 小时记录及清理保留；内核联动测试验证“主事务提交、账本暂未确认”先 503、对账后同请求恢复且作业只有一项。更新后服务端全量集成 **369/369**、删除相关 Playwright **13/13**、全仓类型检查 **13/13**、`spec:check` **6/6**。这些结果未验证正式路由、家庭逐项处置、真实 Apple 撤销、产品 UI 或真机行为，SA-07 / 8.5 保持未完成。

客户端过窗错误采用独立中英文文案，避免把已注销主体引导回登录；契约测试 **56/56**、适配器测试 **170/170**，随后全仓类型检查 **13/13**、`spec:check` **6/6** 和 `git diff --check` 再次通过。客户端未显示正式注销入口。

最后对受理返回值增加账本 `accepted` 状态、主体及意图一致性校验；模拟账本错误回答的受理定向集成 **12/12** 通过。上段全量 **369/369** 的执行在此守卫追加之前，不能将其记为追加守卫后的全量结果。

只读安全审查发现幂等元数据满 24 小时被正常清理后，原请求重试会落回失效会话的 401，错误提示可能误导已注销用户重新登录。现对已无元数据且会话验证失败的删除提交给出专用 `AUTH_DELETION_OUTCOME_UNKNOWN`：明确无法证明旧请求是否受理，不能声称“已完成”；仍不再建作业。同键不同请求的 409 另映射为删除专用冲突提示。两种分支均有中英文文案和定向回归；受理联动 **12/12**、契约 **56/56**、适配器 **171/171**、变更后删除客户端 Playwright **5/5**、全仓类型检查 **13/13**、`spec:check` **6/6**、`git diff --check` 均通过。24 小时之后进度回执仍不可恢复，这是当前 60 秒密文、24 小时元数据保留合同的限制；正式产品流程需给用户可执行的核实渠道。

清理边界审查进一步发现：仅按邮箱地址匹配历史验证码会误删曾持有该地址的其他主体记录；删除等待期可产生无主体的重置验证码；未绑定会话的已验证 Apple 登录流程也可能留存。现在成功注册的验证码在同一事务中绑定新主体；注销清理只按本人主体、本人会话，或本人当前邮箱的匿名重置验证码选取，Apple 流程另按本人外部身份的客户端与 provider subject 匹配。过期验证码保留 24 小时后统一清除，避免无主体历史记录永久留存。隔离 PostgreSQL 清理回归 **18/18**、邮箱回归 **22/22** 通过，包括地址重新分配后不误删前主体、等待期匿名重置被清除、Apple 流程只清本人，以及 24 小时保留边界。服务端全量集成首次 **372/373**、失败输出未保存，紧接着完整复跑 **373/373**，故仍留有一次未定位的并行波动；全仓类型检查 **13/13**、`spec:check` **6/6** 通过。家庭/儿童历史关联的处置仍依赖 13.2 决策，正式注销入口继续关闭。

同一代码状态下，删除客户端与运行时恢复的真实 HTTP Playwright **13/13** 通过，覆盖回执读取、服务端清理调度、账本故障、替换和恢复后拒绝复活。

## 逐家庭选择与冻结规则的增量

维护者确认每个受影响家庭独立选择处置；转交接收人必须已是该家庭有效成人成员并明确接受管理和适用监护责任；唯一管理者选择结束管理且家庭仍有其他成员时，冻结家庭待处理，受影响儿童设备授权立即撤销，共同作品保留待核对。共享 `accountDeletionRequestSchema` 已改为严格 `none` 或非空且家庭 ID 不重复的 `per-family`；受理前的纯校验器将请求家庭集合与当前服务端影响清单中家庭成员和监护关系的家庭 ID 并集精确比较。主库幂等 HMAC 对家庭 ID 排序，同键重排可恢复同一回执，更改任一处置冲突。现有无家庭依赖受理内核复用该校验器，但仍拒绝一切实际家庭依赖。

本增量服务端单测 **49/49**、服务端构建及受理／幂等隔离 PostgreSQL 集成 **22/22**、`spec:check` **6/6**、`git diff --check` 通过。接收人的明确接受记录、受理事务内的冻结与儿童授权撤销、共同作品待核对分支尚未实现；正式注销提交路由继续关闭，不能把合同或校验器计为可用注销。此前的真实 HTTP Playwright **13/13** 属于本增量之前的代码状态。

冻结状态的后续安全切片：0020 增量迁移允许家庭 `frozen` 状态；现有家庭列表和儿童会话验证只认可 `active`，隔离 PostgreSQL 回归证实冻结后其他成员看不到可用家庭、既有儿童 access/refresh 均失效。删除清理器现在也将 `frozen` 家庭的成员与 owner 行视为待处理依赖，保留原家庭和个人资料，作业保持 `needs_attention`、`serverDataDeleted=false`，避免把共同作品待核对阶段误报成删除完成。服务端构建通过，清理与儿童设备集成联合 **22/22**；本增量删除客户端 Playwright **5/5**。这只是状态与防误清理基础：正式注销事务尚未写入冻结状态，也未显式撤销 grant 行或实现后续家庭处理。

内部 `freezeOwnedFamilyForDeletion` 现可在调用方主库事务中执行唯一 owner 的结束管理分支：核对 owner 与剩余有效成员、冻结家庭、显式撤销该家庭儿童 grant 与会话/刷新链、使已批准配对过期、撤回未使用邀请，并结束开放房间及席位；任一步失败由调用方事务整体回滚，监护关系和家庭成员记录留待核对。儿童设备隔离 PostgreSQL **6/6**，覆盖成功、回滚、非 owner／无剩余成员拒绝；清理器仍阻止把冻结家庭误报为已删。该内核未与注销受理事务接线，也没有转交接受记录、后续人工处理流程或公开注销入口。

本切片最终全仓类型检查 **13/13**、OpenSpec 检查 **6/6**、`git diff --check` 通过；这些检查不替代真实 Apple 撤销或客户端注销验收。

## 接收人确认与内部转交切片

0021 增量迁移记录接收人本人对一个家庭管理及适用监护责任的明确接受，只存双方主体 ID、家庭/成员版本、服务端计算的儿童范围摘要和时间；被替代的旧确认保留记录但不可用于转交。有效成人家庭成员可经 `GET /v1/families/{id}/management-acceptance/preview` 读取版本、摘要及儿童数量，再以两项 `true` 明确确认；同状态重复提交返回同一回执。服务端在注销转交事务内重新核对成员资格、家庭/儿童范围和 24 小时期限；过期或变化拒绝，不能仅凭注销者填写接收人 ID 生效。

内部转交事务消耗确认记录、转交唯一 owner 与适用监护关系、撤销原管理者儿童 grant/会话、取消其待处理邀请和配对，并结束其开放房间；事务失败全部回滚。内部注销受理目前可处理全部选择为有效转交的逐家庭请求，仍拒绝任何含结束管理的请求；正式 `DELETE /me/account` 入口关闭。接收记录与转交服务的隔离 PostgreSQL **6/6**、转交接入后的受理集成 **13/13**、接收确认真实 HTTP Playwright **2/2**，服务端和合同构建通过。此切片尚无客户端产品交互、混合家庭冻结处理、旧备份恢复家庭转交一致性证明、真实 Apple 撤销或正式注销验收。

同切片后续校验：接收记录增加被替代标记与 30 天有界清理；共享 API 客户端提供预览和本人确认调用，拒绝伪造身份及无效家庭 ID。合同测试 **60/60**、适配器测试 **173/173**、受理与接收确认的隔离 PostgreSQL 集成 **22/22**、真实 Fastify＋临时 PostgreSQL 的接收确认 Playwright **2/2**，全仓 `typecheck` **13/13**、`spec:check` **6/6**、`git diff --check` 通过。混合“转交／结束管理”在任何家庭或确认记录变更前整体拒绝，尚未接入冻结处置；接收人产品页面与正式注销入口仍未开放。上述验证是本地隔离环境，不能替代旧备份恢复、真实 Apple 撤销或真机验收。

冻结访问边界核查：内部房间容量模块此前只检查家庭 ID 存在，冻结家庭仍可新开房间。现在创建房间时锁定并要求家庭为 `active`，与注销冻结事务串行；冻结或解散后均拒绝新房间。席位认领也先锁定并核对家庭状态，避免旧备份留下的 `open` 房间在冻结家庭内继续接纳设备。隔离 PostgreSQL 房间容量测试 **14/14**；真实 HTTP Playwright 儿童设备路径 **3/3**，包括冻结后儿童会话失效与监护设备管理不可见。房间模块尚无公开 HTTP 创建入口，房间拒绝由其内部仓储测试覆盖；这不等于冻结家庭的后续人工处理已完成。

恢复审查发现：账本只保留注销主体与意图，主库单独恢复到受理前会丢失家庭转交，虽因缺少作业而拒绝启动，但不能自动重建转交；两库同时回退到同一旧点时，现有主库围栏无法识别，尚缺两库之外的独立见证。为防止已有作业或围栏却丢了家庭变更时静默开放，启动恢复现在同时检查 `deletion_pending/deleted` 主体是否仍持有活跃／冻结家庭的 owner、有效成员、监护关系或未过期儿童设备授权；不一致或检查不可读则拒绝开放。隔离双数据库恢复集成 **15/15**，服务端全量集成 **393/393**，真实服务进程恢复 Playwright **9/9**（含新增拒绝启动场景），全仓类型检查 **13/13**、`spec:check` **6/6**、`git diff --check` 通过。这项只检测异常，不恢复家庭转交，也不解决主库与账本联合回退；正式注销入口继续关闭。

运行中漂移补查：就绪检查和 `/v1/` 路由现持续核对主库围栏、独立账本及删除主体的家庭状态；删除作业仍在而主体异常回到 `active` 同样阻断。维护扫描在不一致时暂停，不会继续 Apple 外发或资料清理。合成双数据库恢复集成 **16/16**、服务端全量集成 **394/394**、真实服务进程恢复 Playwright **10/10**，新增用例在同一进程中验证就绪和账号会话从 200→503→200，围栏与账本始终相等。全仓类型检查 **13/13**、`spec:check` **6/6**、`git diff --check` 通过。此检查依赖主库尚存删除作业或主体处于删除状态；两库一起回退且均丢失该记录仍需外部独立见证。

## 混合逐家庭处置与冻结待处理记录

0022 增量迁移新增 `account_deletion_family_reviews`，只保存删除作业、冻结家庭、注销主体、待处理状态和时间；同一家庭只能有一项未结记录。内部受理在同一主库事务内逐家庭执行有效的接收人转交或唯一管理者结束管理冻结，再写注销作业及每个冻结家庭的待处理记录。转交仍要求接收人事先作为有效成人成员明确接受；冻结立即撤销该家庭儿童设备授权及会话、结束开放房间，成员和监护记录保留待核对。若受理前仍有其他家庭依赖或孤立监护同意，整个事务回滚，账本不留下受理标记。

启动和运行中恢复门禁仅在待处理记录与删除作业、`deletion_pending` 主体及 `frozen` 家庭一致时，允许该家庭的 owner、成员、有效监护关联留存；无匹配记录、未撤销的儿童设备授权或孤立监护同意仍阻断开放。清理扫描继续将冻结家庭视为依赖，保持 `serverDataDeleted=false`。隔离 PostgreSQL 的受理／恢复／迁移测试 **39/39**，新增真实服务进程 Playwright **1/1** 验证其他账号可用、注销者会话失效、家庭不可见且待处理作业不被误清理；删除客户端 Playwright **6/6**。该段结果是本地隔离数据，不代表真实 Apple 撤销或上架验收。

待完成：冻结家庭由谁处理及共同作品在待处理期间的访问规则尚待确认；无其他成员家庭的结束管理、非唯一管理者退出、旧备份下家庭转交／冻结重建、主库和账本同时回退的外部见证、客户端可见注销流程及正式提交路由均未闭环。因此 8.5 / SA-07 仍未完成，正式提交入口保持关闭。

同一代码状态复验：服务端完整集成 **403/403**；删除客户端及恢复真实进程 Playwright 合计 **17/17**；全仓 `typecheck` **13/13**、OpenSpec `spec:check` **6/6**、`git diff --check` 通过。上述检查只验证本地隔离环境和合成供应商，没有实际 Apple 撤销或移动端注销验收。

随后仅补充一条数据库回归：注销作业插入失败时，已尝试的家庭冻结、儿童 grant/会话撤销和账本预备标记均回滚或取消；受理单文件 **17/17**。此测试补充未改变产品实现；上段完整集成计数不包含新增测试。

## 非 owner 成人结束本人家庭访问

此前内部受理把所有 `end-family-access` 都送入唯一 owner 冻结步骤，导致非 owner 成年成员无法按现有家庭规则退出。现按服务端锁定读取的家庭角色分派：owner 仍走冻结待处理；非 owner 只停用本人成员资格，撤销本人监护关系与同意、儿童设备 grant/会话、待用邀请、已批准配对和本人房间权限，家庭与其他成员保持 `active`。若本人监护的任一儿童没有另一名当前有效监护人，事务拒绝；跨家庭的监护资格检查锁定关系、同意、主体、家庭与成员行，隔离 PostgreSQL 并发用例覆盖另一家庭同时冻结等变化。受理失败时这些改动全部回滚。恢复门禁另拒绝“家庭仍冻结待处理但作业已标服务端数据删除”的矛盾状态。

先添加的受理用例复现 `AUTH_DELETION_DEPENDENCIES`，接线后通过；内部非 owner 步骤隔离 PostgreSQL **7/7**，完整服务端集成 **412/412**，删除客户端真实 HTTP Playwright **7/7**（验证退出者不可再访问、owner 仍看见活跃家庭、本人资料可由清理作业删除），全仓 `typecheck` **13/13**、`spec:check` **6/6**、`git diff --check` 通过。Playwright 通过内部事务构造已受理状态，正式删除提交 HTTP 路由仍关闭；它不证明客户端完整注销流程、唯一监护人处置、真实 Apple 撤销或真机验收。

## 真正空家庭与已删进度回退

唯一 owner 选择结束管理且家庭从未有其他成员、邀请、监护、儿童设备、配对、房间或其他已建家庭关联时，内部受理现在于同一事务删除本人创建幂等记录、owner 成员行及家庭行。历史停用成员或任一共享关联也会拒绝，避免把“当前无人活跃”误判为空家庭；仍有其他有效成员的家庭继续进入冻结待处理分支。单独空家庭步骤的隔离 PostgreSQL **9/9**、受理层红绿回归以及合成真实 HTTP Playwright 均通过。

清理器现在删除停用的本人家庭成员行；此前 Playwright 稳定复现了进度显示服务端数据已删、数据库却残留本人家庭成员关联，修正后同一用例通过。若部分资料已经清理而后续异常恢复出有效家庭依赖，清理器撤回旧 `local_data_deleted=true`，作业进入 `needs_attention`；隔离数据库红绿用例已验证回执状态同步回退。独立静态审计同时指出，已失效的邀请、监护同意和关系、儿童设备授权、配对、房间等历史记录仍可能带注销者 UUID；它们是否删除、脱敏或按明确期限保留尚未闭环，不能据现有标记宣称账号全部数据已彻底删除。冻结家庭的处理与共同作品规则也仍待完成。

本切片完整服务端集成初跑 **421/423**：新增删除计数字段使旧断言失败，受理测试每例新建账本连接池在第 19 项耗尽隔离库连接。修正期望并复用同一账本池后，两文件联合 **30/30**、完整服务端集成复跑 **423/423**；删除客户端真实 HTTP Playwright **8/8**、全仓 `typecheck` **13/13**、`spec:check` **6/6**、`git diff --check` 通过。未开放正式注销提交入口，也未执行真实 Apple 撤销或原生端注销验收。

## 已终止家庭历史的进度门禁

针对上一段审计发现的剩余关联，清理器现在额外检查解散家庭 owner、家庭邀请、监护同意与关系、儿童设备 grant 与配对、已结束房间、冻结家庭处理记录是否仍指向注销主体。这些记录不会阻止用户提出注销，但在没有明确清除或有界脱敏处理之前，清理扫描保持 `needs_attention`、`serverDataDeleted=false`，也不删除其他成员内容。隔离 PostgreSQL 用“已结束但仍指向本人”的房间先复现错误完成、再验证新门禁；真实 HTTP Playwright 核对回执错误码 `historical_family_data` 且房间仍在。最小主体墓碑、独立防复活账本、限定期限的安全事件及转交接受记录属于单独定义的保留用途，不在本门禁中误判为待删的共享历史。

更新后完整服务端集成 **424/424**、删除客户端真实 HTTP Playwright **9/9**、全仓 `typecheck` **13/13**、`spec:check` **6/6**、`git diff --check` 通过。此门禁只防止错误完成，不替代最终历史数据清理与共同作品核对；用户端正式注销提交和真实 Apple 撤销仍未验收。

后续发现同一历史门禁漏扫 `family_management_acceptances`：记录即使已消费，仍在保留期内保存原 owner 与接收人主体 ID；仅因接收人没有活跃家庭依赖而标记 `serverDataDeleted=true` 会误报。现将双方主体关联纳入 `historical_family_data`，保留记录并让删除作业待处理。隔离 PostgreSQL 清理测试 `node --test apps/server/tests/integration/account-deletion-cleanup.test.mjs` **22/22**，真实 Fastify／临时 PostgreSQL 的 `corepack pnpm exec playwright test tests/e2e/deletion-client.spec.mjs --grep 'retained family management acceptance' --reporter=line` **1/1**，服务端 `corepack pnpm --filter @siyue/server build` 通过。测试用已消费接受记录验证回执仍报服务端数据未删；没有验证正式注销入口或记录到期后的最终清理。

后来补验到期路径：超过接受记录 `retain_until` 后，现有 `cleanupExpired()` 清除该记录，下一次删除清理扫描能够完成；同一隔离测试文件 **22/22**。新增房间邀请 0023 后，注销历史门禁同时核对邀请人／受邀者主体关联；受邀者退出家庭或注销时，邀请记录未结清就保持 `historical_family_data`，不误报服务端数据已删。隔离清理文件 **23/23**，真实 HTTP 删除回执 Playwright 定向 **1/1**；邀请记录的最终保留／清理规则仍未确定，所以这只是安全门禁。

跨家庭监护同意的定向安全修复：内部家庭转交原先会撤回原管理者在所转交家庭引用的同意，即使同一同意记录仍被另一家庭的有效监护关系引用，导致另一家庭的儿童设备会话意外失效。现与非 owner 成员退出步骤保持相同边界：只在没有其他家庭关系引用时撤回同意；转交家庭原管理者的监护关系仍停用，接收人需按既有确认流程承担适用责任。隔离 PostgreSQL 新增转交测试先见 **2/4**、两项因另一家庭同意被撤回失败，修复后 **4/4**；家庭接受 **8/8**、成员退出 **7/7**、注销清理 **23/23**、儿童设备 **6/6** 定向回归通过。该内部步骤没有正式注销 HTTP 入口，未据此声称完整注销验收。

同一代码节点服务端完整隔离 PostgreSQL 集成 **446/446**、OpenSpec 严格校验 **6/6**、`git diff --check` 通过；全量只证明合成数据与服务端路径，未证明正式注销提交、真实 Apple 撤销、移动端或冻结家庭复核。

跨家庭同意修复另补真实 Fastify／临时 PostgreSQL 的 Playwright 回归 `tests/e2e/child-devices.spec.mjs --grep 'transferring one family'` **1/1**：先用家庭 B 的有效儿童设备会话读取账号会话，再将家庭 A 转交给已明确接受的成人，随后同一儿童会话仍返回 200、B 的 grant 与共用同意均未撤销。该测试通过内部转交步骤构造状态，正式注销提交路由仍关闭；结果不代表真实设备或供应商验收。

冻结家庭的同一事务现也将该家庭尚未接受的房间邀请标记撤销；房间本身继续结束，已接受邀请因房间结束不能重新加入。真实 Fastify／临时 PostgreSQL 的冻结儿童设备 Playwright 定向 **1/1**，同时核对儿童会话变 401、grant 显式撤销和待接受房间邀请撤销。冻结家庭后续复核与共同作品访问规则仍待确认。

这一节点服务端完整隔离 PostgreSQL 集成 **450/450**、OpenSpec 严格校验 **6/6**、`git diff --check` 通过；没有进行正式注销入口或真实供应商验收。

转交与非 owner 退出的邀请状态补齐：家庭转交后，旧家庭版本下的待接受房间邀请在同事务标记撤销；非 owner 退出仅撤销本人发出、发给本人或属于本人已结束房间的待接受邀请，其他成员仍有效的房间邀请保持不变。两个新增隔离 PostgreSQL 用例先分别复现 `pending` 未变成 `revoked`；修复后家庭转交与成员退出联合 **13/13**，并追加验证后续事务失败时邀请撤销整体回滚。服务端完整集成在追加回滚断言前 **464/464**，追加后定向 **13/13**。真实 Fastify／临时 PostgreSQL 的 Playwright `tests/e2e/child-devices.spec.mjs -g 'transferring one family|member exit closes'` **2/2**，核对邀请状态、跨家庭隔离和仍有效监护人的儿童会话保持 200。测试通过内部家庭处置构造状态，正式注销提交路由仍关闭；邀请历史清理与已接受邀请的逐人移除规则未由本次决定。

### 客户端提交恢复与受保护回执接线（2026-09-24）

客户端新增注销提交 attempt：严格解析并快照逐家庭处置，响应未知时由用户显式重试同一 bearer、grant、请求体与 UUID；成功回执缓存用于受保护写入失败恢复。账号控制器已接入本人影响预检、密码／Apple 再验证、提交及匿名回执进度。移动端独立 SecureStore key/service/丢失标记与桌面独立 safeStorage 目录已接线，保持原 session vault v1 不变；桌面 IPC 的输入仅包含密码和处置，不接受自报主体、grant 或 token，返回不含回执密钥。

本节点实际运行：attempt 定向 **11/11**；移动 vault 与桌面 IPC **11/11**；桌面 vault **3/3**（确定性加密替身，只验证文件隔离）；Playwright renderer 契约 **9/9**；真实 Electron／临时 PostgreSQL 的现有登录、加密恢复、重启、退出用例新增本人注销预检与空进度查询 **1/1**，使用真实系统 safeStorage。桌面构建、移动与桌面 typecheck、OpenSpec **6/6** 通过。整合后的验证见下段；不以这部分检查声称正式提交或完整注销通过。

正式服务端 DELETE 入口仍关闭，产品逐家庭处置 UI 与 Apple 真授权尚未交付。以上没有执行真实账号删除，没有证明真实供应商撤销、五台真实设备通话或冻结家庭后续复核。

整合完成后，`corepack pnpm --filter @siyue/adapters test` 全包 **219/219**，其中控制器注销 **24/24**、Apple 再验证与提交 attempt 定向 **46/46**。覆盖预检副本不可被调用方篡改、再次验证期间换号不发 DELETE、代际失效后释放旧凭据、原生存储读取超时、回执写入及会话清除失败可重试、另一账号不能读原回执。Apple 注销复用 `delete-account` 授权阶段，提交后固定 bearer/body/key；重试不再弹原生授权。`corepack pnpm --filter @siyue/adapters build` 通过。真实 HTTP 的 `tests/e2e/auth-client.spec.mjs -g "deletion controller previews"` **1/1**：正式路由关闭时提交／重试均不清本地登录凭据、不产生删除作业。

当前限制：完整产品 UI 未启用；提交未知结果的恢复只在同一进程、同一账号代际内有效，进程退出或主动换号会释放待提交凭据，已成功落盘的回执保留。单回执槽在另一笔未到期时拒绝新提交，不静默覆盖证明。真实 Apple、冻结家庭复核、共享及历史数据处置与五设备 RTC 仍未验证，不据此勾选整体 DEL 验收。

最终整合节点再次执行 `corepack pnpm exec playwright test tests/e2e/desktop-auth-renderer.spec.mjs tests/e2e/desktop-auth.spec.mjs tests/e2e/auth-client.spec.mjs -g 'renderer deletion|real Electron main vault|deletion controller previews'` **4/4**（2 项 renderer 契约、1 项真实 HTTP、1 项真实 Electron）。共享包构建后移动端与桌面端 typecheck 均通过；`spec:check` **6/6**、`git diff --check` 通过。没有 commit、push 或发布。

## 注销流程状态与接收人客户端接线

2026-09-24 后续本地工作区节点：新增 `account-deletion-flow.ts`，逐家庭选择默认空，全部明确选择后才进入确认；支持回退修改、密码／Apple 提交、未知结果锁定原请求、显式重试及重启后读取受保护回执进度。表单不保存密码、令牌或回执密钥；账号切换和外部退出清除状态；进度不一致或回执不存在不会显示完成。此文件是共享流程状态，不是已交付的产品页面。

接收人客户端新增本人责任预览与明确接受，桌面 IPC／renderer 接线沿用严格契约；当前成人主体、家庭、三个版本和儿童范围摘要均绑定预览。单次 POST 不自动重试，接受后仍由服务器保留原 owner，不能据此声称已转交。主代理复核发现刷新预览期间仍能接受旧范围：新增回归先以 `Missing expected rejection` 失败，再加入读取／提交互斥、刷新即作废旧预览及回执版本核对后通过。

验证：`corepack pnpm --filter @siyue/adapters test` **260/260**；其中注销流程 **30** 项、家庭责任控制器 **11** 项。`corepack pnpm build:packages` **5/5**。Playwright 的注销流程测试使用真实 Chromium 加合成控制器，验证选择与恢复；家庭接受测试使用真实 HTTP 和隔离 PostgreSQL，覆盖未授权、责任版本变化、显式接受及 owner 不变。产品页面、真实 Apple 和完整注销仍未据此验收。

[流程及政策草案](../design/account-deletion-completion-draft-2026-09-24.md)包含 iPhone／iPad 七页 Figma 总览与可导航原型，待维护者确认。冻结家庭的处理权限与历史资料的清理方式已提出具体选项，尚未收到选择，未擅自新增运营权限、去除历史身份关联或删除共同作品。正式 DELETE 入口仍关闭，8.5 继续未完成。

该节点最终执行 `corepack pnpm exec playwright test tests/e2e/deletion-flow.spec.mjs tests/e2e/family-management-acceptance.spec.mjs tests/e2e/desktop-auth-renderer.spec.mjs --reporter=line` **14/14**（1 浏览器状态流程、3 真实 HTTP 家庭接受、10 renderer 契约）；`corepack pnpm --filter @siyue/mobile --filter @siyue/desktop typecheck` 两端通过；`corepack pnpm spec:check` **6/6**；`git diff --check` 通过。桌面 `node --test apps/desktop/src/main/auth-ipc.test.mjs` 此轮 **8/8**，验证参数边界。未提交或发布。
