# 工作包清单

> 阅读方式（2026-09-13）：以下日期条目保留历史工作包及验收事实，不是当前正式导航或排期的完整快照。当前版本范围见 [版本计划](../planning/releases/0.0.1.md)，移动入口见 [DESIGN.md](../DESIGN.md)。2026-09-06“正式目标页面需后续重新设计”的说明已由后续个人空间实现记录更新；这不提升任何工作包或跨端验收状态。后续只更新状态摘要、剩余条件与证据链接，不继续复制运行日志。


共 20 项：SY-001 至 SY-018 为初始发布路径，SY-019/020 为后续扩展。状态随实际证据更新；这些不是已经创建的 GitHub Issues。2026-09-06：SY-001/002 与本地 Mock 范围的 SY-006 已完成，M1 跨平台验收仍未完成。

## SY-001 · 建立三端工程骨架与版本记录

里程碑：M1 | 优先级：P0 | 状态：done

本轮证据：[M1 验证记录](evidence/m1-validation.md)。锁文件、官方版本对齐、类型检查、共享/服务端/桌面构建、移动双平台JS打包已验证；iOS模拟器原生构建通过，两条独立UI用例分别执行通过，覆盖草稿闭环及手动改名/完成/归档重启；iPhone开发签名与两条核心真机UI路径分别通过，故障/后台场景未验收；Android SDK/专用模拟器与独立UI宿主已就绪，主应用重试构建、APK签名校验及安装通过，Android模拟器两条独立UI用例分别通过（各1项0失败，336.319/325.679秒），第二条验证真实断网下手动改名/完成/归档与重启；Windows未运行。 iOS/Android独立QA真实丢回执与跨进程原回执对账分别通过（43.191/12.053秒，各1项0失败），恢复execute=0且pending验证后清除；各与本平台两条正常UI用例分别统计，不覆盖正常用户UI错误交互、断电、生成取消/后台中断、升级或真机。 iPhone 16 Pro Max（iOS 26.6.1 / 23G83）已恢复USB连接，两条核心UI路径分别有通过证据：草稿闭环142.692秒、改名/完成/归档重启125.001秒；首轮整套为1通过1失败，不能写同次2/2。

依赖：无

验收：
- 采用 pnpm workspace；移动为 Expo，桌面为 Electron + React，服务端为 Node。
- 记录 React/Expo/RN/Electron/Node 的版本与可执行命令。
- 完成当前可用环境的构建；iOS/Android 真机与 Windows 未运行项如实标注。

## SY-002 · 定义纯 TypeScript 领域与命令契约

里程碑：M1 | 优先级：P0 | 状态：done

本轮证据：[M1 验证记录](evidence/m1-validation.md)。纯TS领域/运行时契约与命令服务已实现，共享审批/版本/幂等/空间隔离行为测试通过。

依赖：SY-001

验收：
- Goal/Project/Task/Draft/Receipt 具备 schema 与行为约束。
- 共享层不导入 UI、Node 文件系统、Electron 或具体数据库驱动。
- 同一命令可从手工 UI 与 AI 路径调用；有跨空间引用拒绝测试。

## SY-003 · 验证 SQLite 与同步候选并记录 ADR

里程碑：M1 | 优先级：P0 | 状态：in_progress

本轮证据：[M1 验证记录](evidence/m1-validation.md)。Node隔离PoC、实际Electron SQLite重启及expo-sqlite在iOS模拟器上的目标闭环与重启持久化通过；另一次独立UI运行验证目标/任务改名ID不变、完成后归档与重启同ID/状态/数量保留。Android环境与独立UI宿主已就绪，主应用重试构建并安装成功，Android模拟器UI已验证草稿保存、确认创建、任务完成与force-stop重启同ID/状态/数量保留；Android另一次独立UI用例在关闭Wi-Fi/移动数据且无默认网络时验证手动改名同ID、完成归档和force-stop重启保留，网络已恢复；Android真机及iPhone故障场景、PowerSync和退出演练仍未验证。 iOS/Android独立QA真实丢回执与跨进程原回执对账分别通过（43.191/12.053秒，各1项0失败），恢复execute=0且pending验证后清除；各与本平台两条正常UI用例分别统计，不覆盖正常用户UI错误交互、断电、生成取消/后台中断、升级或真机。 本轮iOS/Android独立原生存储QA分别通过（61.387/2.891秒，各1项0失败）：真实SQL写入后抛错回滚、关闭重开保持原快照、同命令重试与重复投递幂等；损坏JSON和未来版本经createNativeClient拒绝，原schema/rows/version保留。该close/reopen测试不等于跨进程、物理断电、生产迁移或正常用户UI验收；iOS首轮输入漏字未进入SQL，修复输入等待后通过，旧日志保留。

依赖：SY-001, SY-002

验收：
- 在隔离原型验证 RN 与 Electron 的持久化、事务和原生构建。
- 比较 PowerSync 与受限备选的冲突、账号隔离、许可和退出路径。
- 写 ADR-004 的采用/不采用结果及版本证据；不以 demo 代替生产同步验收。

## SY-004 · 实现本地存储、事务与升级恢复

里程碑：M1 | 优先级：P0 | 状态：in_progress

本轮证据：[M1 验证记录](evidence/m1-validation.md)。单设备快照事务、损坏/未知版本拒绝、重启恢复已实现；iOS模拟器两条独立UI用例分别验证草稿/正式记录/完成状态重启保留，以及目标/任务改名ID不变、任务完成后归档、重启同ID/归档状态/数量保留。Android模拟器草稿恢复及正式记录/任务完成状态跨force-stop重启保留通过；Android第二条独立UI用例在真实断网下验证改名同ID、完成后归档、重启ID/名称/状态/数量保留及归档只读，网络已恢复；正式迁移、删除标记、正常用户UI错误交互、取消/升级故障与Android真机/iPhone故障场景仍待验收。 iOS/Android独立QA真实丢回执与跨进程原回执对账分别通过（43.191/12.053秒，各1项0失败），恢复execute=0且pending验证后清除；各与本平台两条正常UI用例分别统计，不覆盖正常用户UI错误交互、断电、生成取消/后台中断、升级或真机。 本轮iOS/Android独立原生存储QA分别通过（61.387/2.891秒，各1项0失败）：真实SQL写入后抛错回滚、关闭重开保持原快照、同命令重试与重复投递幂等；损坏JSON和未来版本经createNativeClient拒绝，原schema/rows/version保留。该close/reopen测试不等于跨进程、物理断电、生产迁移或正常用户UI验收；iOS首轮输入漏字未进入SQL，修复输入等待后通过，旧日志保留。

依赖：SY-002, SY-003

验收：
- 业务数据与活动/命令回执按选定适配器一致提交。
- 重启不丢失；升级失败不覆盖唯一原始数据。
- 测试重复命令、部分失败、删除标记与恢复入口。

## SY-005 · 建立秘密存储、数据授权与日志边界

里程碑：M1 | 优先级：P0 | 状态：in_progress

早期 Mock 阶段证据：[M1 验证记录](evidence/m1-validation.md)。受限IPC、参数和空间权限、脱敏本地服务通过；平台秘密存储与生产服务鉴权尚未实现。 本轮默认本地Mock安全源码审查与73项指定测试通过（mobile4/desktop13/server7/adapters40/ai9，不含未重跑domain/contracts）。修复移动未知error.code回显、原型键命中和对象String副作用，未知错误改固定提示；RED1通过3失败、GREEN4通过0失败，typecheck通过。默认无BYOK/真实Provider，平台秘密存储与生产鉴权对当前分支不适用且未实现；pending hash非加密。移动双平台Hermes JS导出通过，脱敏改动尚无原生UI故障注入回归，先前原生通过属于修改前构建；其余正常UI错误恢复、完整包体/网络审计仍待验收。详见docs/evidence/m1-security-review.md。

2026-09-06 基础框架补充：移动端增加个人 OpenAI 兼容 BYOK、SecureStore 安全配置、明暗主题设置与流式聊天。属于 SY-021；真实供应商、Android 原生和真机仍待验收。目标草稿仍采用原有 Mock，生产鉴权未实现。见 docs/evidence/mobile-ai-settings.md。

依赖：SY-001, SY-002

验收：
- 区分本地保存、同步、AI 外发与后台用途。
- 模型平台 Key 不进入 App；个人 Key、日记、录音不进入日志或 Git。
- 受限 IPC/服务端鉴权接口有非法参数与越权测试。

## SY-006 · 接入 AI SDK 候选与 Mock Provider

里程碑：M1 | 优先级：P0 | 状态：done

本轮证据：[M1 验证记录](evidence/m1-validation.md)。维护者明确选择先完成本地Mock；确定性文本草稿、取消/失败、SDK结构化输出与工具审批离线契约通过。未配置或调用真实模型。

依赖：SY-001, SY-002, SY-005

验收：
- 确定性 Mock 能覆盖文本、工具、审批、失败与取消。
- 实际模型凭据由部署者提供；使用前配置供应商和预算。
- 能力不足明确返回错误，不自动换未授权供应商。

## SY-007 · 实现 AgentRun 持久状态与事件协议

里程碑：M1 | 优先级：P0 | 状态：in_progress

本轮证据：[M1 验证记录](evidence/m1-validation.md)。已持久记录Mock运行状态/seq/策略/数据截止；事件含schemaVersion/runId/seq，授权按游标补读、旧格式兼容和只读重放测试通过；iOS模拟器保存草稿后重启、显式继续与确认通过，Android模拟器保存草稿后force-stop、重启显式继续与确认通过；Android独立QA复用正常HomeScreen已通过生成取消/进程强杀恢复（47.61秒，1项0失败）：取消后超过20秒Mock期限仍无迟到写，第二run经两次force-stop重启保留同runId/interrupted/seq2且无新增事件。iOS同QA也独立通过（269.349秒、1项0失败），语义与Android一致；两平台分别运行，同进程系统后台中断未测。

依赖：SY-004, SY-006

验收：
- 事件带 runId、seq 与 schemaVersion。
- 断流、取消、重启和未知结果分别处理，不伪造完成。
- 记录模型/策略版本与数据截止范围，不持久化隐藏思维链。

## SY-008 · 实现草稿、审批与幂等业务执行

里程碑：M1 | 优先级：P0 | 状态：in_progress

本轮证据：[M1 验证记录](evidence/m1-validation.md)。审批绑定身份/hash/对象版本/有效期，拒绝/过期/撤权/重复/丢回执已测；桌面真实全进程重启稳定命令已验证，移动独立SQLite待提交日志和重开对账已实现并经Node测试。iOS模拟器草稿编辑保存、重启后显式确认和拒绝不增记录通过；Android模拟器草稿编辑保存、重启显式确认及拒绝不增记录通过；正常用户UI错误交互与Android真机/iPhone故障场景仍待验收。 iOS/Android独立QA真实丢回执与跨进程原回执对账分别通过（43.191/12.053秒，各1项0失败），恢复execute=0且pending验证后清除；各与本平台两条正常UI用例分别统计，不覆盖正常用户UI错误交互、断电、生成取消/后台中断、升级或真机。

依赖：SY-004, SY-005, SY-007

验收：
- 确认绑定参数 hash、对象版本、space 与有效期。
- 拒绝/过期/撤销不写正式表；参数变化需新审批。
- 相同 commandId 返回同一结果，丢回执后先对账再重试。

## SY-009 · 完成目标—项目—任务的最小闭环

里程碑：M1 | 优先级：P0 | 状态：in_progress

本轮证据：[M1 验证记录](evidence/m1-validation.md)。目标草稿编辑确认、手动保存、改名/任务完成/归档已接入两端；实际macOS桌面闭环通过。iOS模拟器两条独立UI用例分别执行通过：草稿编辑保存、重启显式继续、确认目标/项目/任务、任务完成与重启保留、拒绝不增记录、手动创建；另一用例验证手动目标/任务改名ID不变、任务完成后归档、重启同ID/状态/数量保留。上述iOS用例未主动切换模拟器网络或注入断网；iPhone开发签名与两条核心真机UI路径分别通过，故障/后台场景未验收；Android环境与独立UI宿主已就绪，主应用构建、APK签名校验及安装通过；Android模拟器UI闭环通过（1项、0失败，336.319秒），覆盖草稿编辑保存、重启显式继续、确认目标/项目/任务、任务完成后重启同ID/状态/数量保留、拒绝不增记录和手动创建。Android第二条独立用例在关闭Wi-Fi/移动数据且无默认网络时通过（1项0失败，325.679秒），验证手动目标/任务改名同ID、旧名全页不存在、完成归档、重启同ID/名称/状态/数量及归档只读；网络已恢复。两条Android用例分别运行，不是同次2/2；Android真机、iPhone故障/后台及正常用户UI其余错误交互/升级故障仍待验收。 iOS/Android独立QA真实丢回执与跨进程原回执对账分别通过（43.191/12.053秒，各1项0失败），恢复execute=0且pending验证后清除；各与本平台两条正常UI用例分别统计，不覆盖正常用户UI错误交互、断电、生成取消/后台中断、升级或真机。 iPhone 16 Pro Max（iOS 26.6.1 / 23G83）已恢复USB连接，两条核心UI路径分别有通过证据：草稿闭环142.692秒、改名/完成/归档重启125.001秒；首轮整套为1通过1失败，不能写同次2/2。

依赖：SY-004, SY-008

验收：
- AI 生成可编辑草稿；确认后出现真实对象。
- 手动创建、编辑、完成和归档在离线状态可用。
- iOS 核心流程、Android 同一路径与 Electron 最小入口具备验收证据。

## SY-010 · 实现活动时间线和可管理记忆

里程碑：M2 | 优先级：P0 | 状态：planned

依赖：SY-004, SY-005, SY-009

验收：
- 正式活动与审计分开；数字从正式数据计算。
- 候选记忆与确认事实分开；用户可编辑和删除。
- 撤销来源后相关摘要/检索投影失效。

## SY-011 · 验证语言学习录音与音频中断

里程碑：M2 | 优先级：P0 | 状态：planned

依赖：SY-001, SY-005, SY-006

验收：
- iOS/Android 真机录音、回放、权限拒绝、耳机/中断有实际记录。
- 无录音权限时文字路径可完成练习。
- 音频外发用途明确；ASR 不宣传成客观发音评分。

## SY-012 · 实现学习会话、纠错候选与复习队列

里程碑：M2 | 优先级：P0 | 状态：planned

依赖：SY-009, SY-010, SY-011

验收：
- 练习关联目标或项目；原始会话与模型评估分开。
- 词汇与纠错以候选展示，确认后进入复习。
- 重复上传/提取不重复生成已确认项，时长由计时记录。

## SY-013 · 实现有来源的用户触发复盘

里程碑：M2 | 优先级：P0 | 状态：planned

依赖：SY-010, SY-012

验收：
- 统计与模型文本分开，提供记录依据与覆盖范围。
- 未同步、缺失或不足的数据明确说明。
- 复盘可修改保存；不自动成为新个人事实或新任务。

## SY-014 · 实现可选身份绑定与生产同步

里程碑：M3 | 优先级：P0 | 状态：planned

依赖：SY-003, SY-004, SY-005, SY-008, SY-009

验收：
- 账号绑定、切换、撤权与数据空间不串号。
- 本地乐观状态与服务端裁决有清楚 UI；拒绝保留可恢复输入。
- 重复投递、并发编辑、删除复活和旧客户端重建用例通过。

## SY-015 · 完成数据导出、恢复与级联删除

里程碑：M3 | 优先级：P0 | 状态：planned

依赖：SY-010, SY-012, SY-014

验收：
- 导出带版本、范围、附件清单及缺失说明。
- 导入预览去重；账号删除覆盖正文、附件、记忆和索引。
- 备份过期与删除重放规则被文档化并验证。

## SY-016 · 完成核心回归、安全与费用边界测试

里程碑：M3 | 优先级：P0 | 状态：planned

依赖：SY-013, SY-014, SY-015

验收：
- 执行 planning/test-cases.json；记录通过/失败/未运行和证据。
- 关闭授权、模型故障、预算耗尽不突破边界。
- 不将 mock 成功等同真实模型或真机成功。

## SY-017 · 完成开源许可、依赖与发布范围检查

里程碑：M4 | 优先级：P0 | 状态：planned

依赖：无

验收：
- 维护者确认 LICENSE、贡献和公开范围后才应用。
- 复用代码与所有直接/关键间接依赖有来源及许可记录。
- 没有真实用户数据、生产地址凭据或误导性商标/域名声明。

## SY-018 · 打包可复现 Alpha 并记录发布证据

里程碑：M4 | 优先级：P0 | 状态：planned

依赖：SY-016, SY-017

验收：
- 全新环境可按文档启动相应平台；已验证与未验证平台分开列。
- 工件对应明确版本和 commit；功能表与实际一致。
- 公开、签名凭据及托管支出经维护者明确授权后执行。

## SY-019 · 增加手工资产账户与快照

里程碑：M5 | 优先级：P1 | 状态：planned

依赖：SY-018

验收：
- 金额/数量精度与币种明确，估值有时间和来源。
- 内部转账、收入、估值变化不混为一谈。
- 金融域授权隔离；不自动交易、不宣称监管或收益保证。

## SY-020 · 验证托管 AI、后台任务与权益服务

里程碑：M5 | 优先级：P1 | 状态：planned

依赖：SY-018

验收：
- 先完成预算、认证、限流、平台购买与目标市场审查。
- 后台运行只使用已授权已同步数据，任务可取消并对账。
- 权益统一接口；不能因订阅失效阻断个人数据导出。

结构化版本：[planning/backlog.json](../planning/backlog.json)。任务创建或完成时分别填写 GitHub 编号和实际证据，不自动推定。

## SY-021 · 补齐移动 AI 基础对话与个人密钥设置

里程碑：M1 | 优先级：P0 | 状态：in_progress

依赖：SY-001, SY-002

维护者于 2026-09-06 要求优先补齐基础框架，选择通用 OpenAI 兼容接口；在 SY-009 剩余验收之前先完成本切片。同步推进 SY-005 的移动个人密钥部分，不因此宣称 SY-005 全部完成。

验收：
- 明暗主题、服务地址、模型与个人密钥可在设置页管理；密钥仅安全存储，改地址不能沿用旧密钥。
- 当前会话文字经用户发送到指定 OpenAI 兼容服务，流式回复可停止、重试，配置切换不带入旧会话。
- 连接测试仅发送合成提示，失败与取消可恢复；至少一项真实供应商调用与 iOS 真机验证单独记录。

代码与单元验证已落地；真实供应商凭据未提供，不能将合成测试写为真实 AI 验收。详见 [移动 AI 与设置验证](evidence/mobile-ai-settings.md)。


### 2026-09-06 设置改版进展

SY-009 仍为 in_progress。2026-09-06：维护者要求移除初始化示例。移动正式导航现为对话/设置，目标与行动示例及原生组件预览入口已移除；旧目标 UI 仅保留为隔离 QA fixture，数据库与业务代码未删除。历史目标 UI 验收属于旧界面，不能证明当前正式移动入口可用。正式目标页面需后续重新设计。

SY-021 仍为 in_progress：六家国内服务预设、自定义接口和分层设置已实现；iOS 模拟器明暗截图、供应商搜索/选择、地址展开及空密钥校验通过。移动 38 项测试、类型检查、双平台 JS 导出通过；真实供应商和真机尚待验证。见 [验收记录](evidence/settings-redesign.md)。


## SY-022 · 验证五设备视频白板的本地可编辑存档与服务边界

里程碑：0.0.1 | 优先级：P0 | 状态：in_progress

依赖：SY-001, SY-002；仅复用已存在的工程基础，不等待旧工作包全部验收。

对应 [add-family-video-whiteboard](../openspec/changes/add-family-video-whiteboard/proposal.md) 2.1/2.4。本包只负责技术验证；正式应用接线、房间权限与真机验收仍按该变更后续任务执行。

验收：
- 在隔离合成数据上验证题图、可编辑笔迹与页面跨进程保存重开，不以截图代替作品。
- 原子写入中断、损坏、未来版本和附件缺失均保护最近完整原件，明确未验证的原生边界。
- 核对国内 RTC/白板官方能力、临时留存、导出与费用口径；具体厂商和数据用途由维护者批准。

未创建 GitHub Issue；不以规划编号表示远端任务已存在。

SY-022 本轮进展与实际验证见 [本地交接证据](evidence/video-whiteboard-local-2026-09-20.md)；AT-025 仍为 partial，正式应用与真机未验收。

## SY-023 · Excalidraw 白板本机编辑与恢复

里程碑：0.0.1 | 优先级：P0 | 状态：in_progress

依赖：SY-001, SY-002。维护者要求白板先于真实通话独立试用，对应 VWB-06 / 任务 6.x。

验收：

- 无需通话，从侧栏进入空白页/示例题图，画笔、整笔擦除、撤销重做及多页可操作。
- 本机可编辑保存后重开；损坏或未来版本拒绝覆盖，保存失败保留当前笔迹。
- 双语及 iPhone/iPad 原生交互验证分别记录，不将本机原型计为正式五设备协作验收。

实现和验证见 [独立白板试用证据](evidence/whiteboard-trial-2026-09-20.md)。这是待维护者评审的可操作原型；正式协作 UI 设计及发布门槛不变。未创建远端 Issue。

SY-023 当前由 C12 / VWB-07 / VWA-05 / 任务 7.x 推进 Excalidraw 编辑层替换。以 [Excalidraw 接入证据](evidence/excalidraw-integration-2026-09-20.md) 为当前实现与验证入口；上面的旧自研试用步骤仅作历史。工作包仍为 in_progress，真机与正式协作门槛不变。

## 2026-09-21 · SY-005 / SY-022 身份基础进展

独立 PostgreSQL 与会话内核首批实现和本地验证见[证据](evidence/backend-auth-foundation-2026-09-21.md)。两个工作包保持 in_progress；邮件、Apple、客户端、家庭/五设备与生产仍未完成。

2026-09-21 SA-04：邮箱服务本地实现和失败／并发验证见[证据](evidence/backend-auth-email-2026-09-21.md)。SY-005／SY-022 继续 in_progress；真实邮件、客户端、Apple、家庭及部署仍待完成。

2026-09-22 SA-05：客户端会话协调、安全存储适配与受限 IPC 已接入，真实 HTTP 和 macOS Electron 验证见[证据](evidence/backend-auth-client-2026-09-22.md)。登录表单、账号空间映射及移动原生安全存储验收仍未完成；SY-005／SY-022 保持 in_progress。

同日后续：设置 → 账号已接入登录和找回密码，桌面真实流程及 iPhone/iPad/Android 原生登录恢复通过；完整 UI 矩阵、注册与账号空间映射仍待完成，见[界面验证](evidence/backend-auth-account-ui-2026-09-22.md)。SY-005／SY-022 仍为 in_progress，不以本地通过代替真实邮件、Apple 或生产验收。

2026-09-23～24 SA-07 增量：本人设备会话有界分页、单设备及全设备撤销 API 使用动作绑定 reauth grant，验证主体隔离、重复撤销和审计；桌面及移动账号页接入设备列表和撤销交互，桌面中英真实界面通过，iPhone 模拟器中英/明暗设备列表、本机撤销、英文其他/全部设备撤销与冷启动不恢复通过；Android API 36 中英其他/全部设备撤销及冷启动不恢复各 1/1 通过。后续审查修复异常 grant 误报 503、撤销全部响应丢失后的本机恢复记录收敛，并以追加迁移区分审计行为者/目标。证据见[设备会话管理](evidence/backend-auth-device-sessions-2026-09-23.md)。Apple-only 的服务端 `link-identity` 重新验证已接入本地合成验证，客户端/iPad/真机、注销与删除回执未完成；SA-07 和 SY-005 继续 in_progress。

2026-09-24 SA-07 绑定增量：[Apple 重新验证与邮箱绑定](evidence/backend-auth-identity-link-2026-09-24.md)已完成服务端、严格共享 API client、认证控制器及本地合成联合旅程；从 Apple-only 主体取得动作绑定 grant、验证新邮箱并建立密码，到 Android 平台邮箱登录仍是同一主体。控制器的丢响应重试和换号取消已测；产品账号页未接入该流程，真实 Apple/邮件、解绑和注销仍未验收，SY-005 继续 in_progress。

SA-05 6.4a：独立空间映射、会话驱动协调器和旧客户端生命周期已完成基础实现，真实服务＋SQLite双账号测试及回归通过，见[空间基础证据](evidence/backend-auth-account-space-2026-09-22.md)。正式宿主／UI、白板命名空间及原本机空间显式关联尚未接线，6.4整体与工作包状态不提升。

2026-09-23 SA-05/邮箱客户端增量：已补改密的受限 IPC、移动共享控制器、中英账号界面与未知结果同幂等 key 恢复。当前切片的桌面验证及移动未验收边界见[改密客户端闭环证据](evidence/backend-auth-password-change-client-2026-09-23.md)；注册条款门控、原生改密验收和 SA-05 整体验收仍未完成。

2026-09-25 SA-07 注销补全：已落实维护者授权的逐家庭处置、冻结运维复核、分类历史清理与已确认 iPhone/iPad 页面；独立账本配置完整时正式提交可用，桌面和移动共用受保护回执恢复。[本轮证据与限制](evidence/backend-auth-deletion-completion-2026-09-25.md)取代前述历史“路由关闭／页面未接线”结论。SA-07/SY-005 整体状态保持 in_progress，不以本地合成验证代替真实供应商与完整平台验收。

2026-09-25 真实线上账号链路：[线上证据](evidence/online-account-lifecycle-2026-09-25.md)记录公网部署、真实收件注册、Electron 同主体重启恢复/再登录、正式注销完成及注销后登录拒绝。iPhone/iPad 中英文隔离原生注册、冷启动恢复和注销亦已通过；受控输入回写错位已修复，失败与最终连续复跑结果保留在同一证据中。该当前结果替代前述历史“真实邮件及注册未完成”；工作包与整体平台矩阵仍保持进行中，Apple、多人家庭和真机不据此升级。
